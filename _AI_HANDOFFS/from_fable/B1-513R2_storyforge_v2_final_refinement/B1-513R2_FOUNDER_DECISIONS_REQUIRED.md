# B1-513R2 Founder Decisions Required

Inherited decisions stand as written: FD-1 (consent wording) and FD-2 (visibility default) from B1-513; FD-R2 (external guest/email copy), FD-R3 (RA canary person), FD-R4 (Avatar Studio readiness) from B1-513R. Pass 2 re-anchors the main gate and adds one small new decision.

## FD-R1 (re-issued for pass 2) — V2 prototype product approval · THE final gate

Walk `B1-513R2_FINAL_WORKING_PROTOTYPE.html` in Chrome (you land as yourself). The pass-2 walk, in order:

1. **Home** — the Dr Brian Recommends strip under the capture bar.
2. **Inspiration** — list-first reading, `LIST | GRID`, pin a question (📌 vs ★), drag or ↑↓ your pinned order, answer one inline.
3. **Request a Story** — the four-step process strip; press **＋ Ask someone**, fill it, and confirm that nothing sends until the full-email preview's `CONFIRM & SEND`; read the lifecycle chips on the seeded invitations (including the bounced one).
4. **See their view** on Rosa (parent journey), Sam (friend journey), Dr. Ito (mentor journey) — three different question paths, same simple guest surface.
5. **Settings** — switch to **Light**, then walk Home/Library/Inspiration in light; try Ember Storm and confirm it follows you across routes; set **Auto** if you like.
6. **Administrator View** — Home's four counts + "Today, in order"; Students at 122 with search/session/sort/saved views/pages; the Queue's oldest-first answer; a mirrored review in Maya's Story Room; **Content Studio** tabs, Add one question, and the CSV bulk import (parse → review → save drafts — note it cannot publish from the upload).

**The Codex megarun prompt becomes executable authority only after this approval.** Any surface you amend, we fold in before Codex.

## FD-R5 (new, small) — Dr Brian Recommends: label + launch picks

The recommendation voice is literally you. Approve (a) the label wording — currently **"✪ Dr Brian recommends"** with the Home sub-line "questions picked for this cohort" — and (b) which questions carry the mark at launch (the prototype seeds three as a demonstration). One sentence back is enough; this blocks nothing except the recommend marks themselves.

## FD-R2 note (unchanged gate, new surface)

The pass-2 email preview now shows the exact From-identity question inline: production must **verify** the sending identity against the live Postmark server (USCE precedent: `clinicals@missionmedinstitute.com`) rather than assume `storyforge@…` exists. Your FD-R2 wording approval should include the visible From name.

### Explicitly NOT Founder decisions (resolved in-package, recorded)

List-first default and layout persistence (§7, implemented) · pin/favorite semantics and student-owned order (§8) · bulk-import safety flow and never-publish-from-upload (§10, red-team hardened) · truthful lifecycle vocabulary incl. "Opened (approximate)" and terminal bounce (§14, red-team hardened) · relationship journey composition (§15, from the governed contributor library) · admin scale mechanics: filters, saved-views-as-state-only, pagination caps (§18–22) · Content Studio tab set (§25) · page-introduction copy (§26) · environment energy split and labels (§30) · light-theme design language (§31, yours to amend at FD-R1 like any surface) · V2.1 prewiring scope (§40C — fixed by the master prompt, not by preference).

# B1-506A · Fable Authority Amendment · StoryForge V5.5 Phase 1

Binding authority amendment issued by Fable under founder directive B1-506A. Scope: exactly the six Codex-reported blockers (FABLE-1 to FABLE-6 in `B1-506_COMPLETE_COMBINED_HANDOFF.md` Section 7). Nothing else in B1-504A, B1-504B, or B1-505C is reopened. Evidence labels: VERIFIED SOURCE TRUTH (VST, read from the repository this run), VERIFIED RUNTIME OR TEST TRUTH (VRT, from committed test receipts and B1-505D probe evidence), BINDING AMENDMENT (BA), REQUIRED EXTERNAL PROBE (REP), FOUNDER GATE (FG), STILL BLOCKED (SB).

Companion document `B1-506A_EXECUTABLE_SQL_AND_CONTRACTS.md` carries the exact SQL and contracts; where this document and that one describe the same object, the SQL document's literal text governs.

Baseline verified this run (VST/VRT): worktree HEAD `411b7aeedced351cf15c1e25601a7714c119d1fa` (handoff commit) over implementation commit `de1828ccb2c3672cc66b39e1ecbf46d1bfda5d1b` on `codex/b1-503-storyforge-product-recovery`; current M1 SHA-256 `b175549e4f2e1606badccdd194f25e42a11b3954f95b435e0b75ebfb52d2cc5f`, M1 rollback `669f6c2404222d07217dc6cd47c1eab57c52cdc70a6af20571a85ef347f5dca5`, M2 `8899d7d6525c0cbc72790378fcf6a2d8aeb4bc1e7b8afac737be6c3e9af34c3a`, all re-hashed directly on device and matching the Codex handoff.

## Amendment 1 · Student-only M1 RLS · APPROVED

- VST: current M1 policies call `sf_has_live_identity()` with the default NULL role; the helper (b1_502 migration, lines 21 to 38) accepts any eligible live role when the argument is NULL. The M1 comment "students only" contradicts its own predicates.
- VRT: the four committed PostgreSQL denial cases (mentor self-row, admin self-row, student-to-mentor closure, student-to-admin closure) are red against unchanged M1, exactly as the amendment request states.
- BA: the four predicate substitutions in `B1-506A_FABLE_AMENDMENT_REQUEST.md` lines 26 to 43 are APPROVED VERBATIM: `public.sf_has_live_identity(ARRAY['student'])` in the USING and WITH CHECK of `sf_recording_sessions_rw` and in the USING and WITH CHECK of `sf_recording_segments_rw`. No other character of M1 changes.
- BA: amended M1 SHA-256 `6f6a3340bc29d1222b5f78472eb9a4897739722d090241de6d64f3e8f781c9c2`. Fable INDEPENDENTLY REPRODUCED this hash by applying exactly the four substitutions to the current on-device M1 and hashing the result. Codex's checksum arithmetic is confirmed correct.
- BA: migration filename unchanged (`20260729000100_b1_506_voice_recording_sessions.sql`, amended in place; no protected environment has applied it, per the migration-ledger evidence and the fact that S7 never executed). The three runner pins in `scripts/apply-production-migrations.sh` (expected_hashes, first ledger VALUES set, reverse-comparison ledger VALUES set) are replaced with the amended hash. The already-applied contingency procedure in the amendment request (corrective additive migration, non-student row count, no ledger rewriting) is ratified as the standing rule should evidence ever show a protected application.
- BA: rollback effect unchanged. The existing rollback drops both tables and encodes no predicates; its hash `669f6c24...` stays pinned.
- BA: no additional grant or policy change is required for this amendment. Mentor and admin content access is denied by the ABSENCE of any matching policy under FORCE ROW LEVEL SECURITY, now made explicit by the student-only predicates: their inserts fail with SQLSTATE 42501 and their reads return zero rows. The `storyforge_app` service policies and grants are untouched and gain nothing; RP-13 (VRT) confirms the service connection is the dedicated role `storyforge_app` with NOSUPERUSER, NOBYPASSRLS, NOINHERIT, satisfying the B1-505C posture bound.
- Tests that must turn green: the four expected-red cases in `tests/postgres/recording-rls.test.mjs`. Tests that must not change: student-owner access, cross-student zero rows, ineligible zero rows, service worker/purge access.

## Amendment 2 · Bounded audit writer · RULED

- VST: `sf_append_audit` (b1_503 conformance migration line 483) is SECURITY DEFINER with EXECUTE revoked from PUBLIC and granted to no role; `sf_audit_events` has no INSERT grant to any role and an append-only trigger. `server/db.mjs` maps SQLSTATE 42501 to `audit_writer_unavailable` 503, which is the exact cause of the one expected e2e red (cancel fails closed).
- BA: two new bounded SECURITY DEFINER functions are created (exact SQL in the companion document): `public.sf_append_voice_audit(...)` with EXECUTE granted to `authenticated` ONLY, and `public.sf_append_voice_audit_service(...)` with EXECUTE granted to `storyforge_app` ONLY. The disjoint EXECUTE grants ARE the caller-identity mechanism: privilege is checked against the role in effect at call time (`SET LOCAL ROLE authenticated` vs `SET LOCAL ROLE storyforge_app` in db.mjs), and RP-13's NOINHERIT attribute (VRT) guarantees the login role does not inherit `authenticated`'s privileges. `sf_append_audit` itself remains ungranted and untouched.
- BA, bounds shared by both writers: fixed event vocabulary partitioned per writer (authenticated: recording_started, segment_received, recording_finished, audio_deleted, audio_attached, unauthorized_denied, feature_scope_changed; service: recording_cancelled, recording_swept, assembly_completed, assembly_failed, segment_transcribed, segment_transcribe_failed, provider_failover, reconciliation_deleted, object_delete_retried); entity types recording_session, recording_segment, audio_asset, feature_flag; surfaces quick, library, system (service writer: system only); payload guard rejecting any jsonb that is not a flat object with whitelisted keys, scalar or small-array values, bounded lengths, and a valid errorCategory when present; role checks inside the authenticated writer (feature_scope_changed requires admin; recording actions require student; unauthorized_denied any live role); actor derivation from `sf_actor_id()`/`sf_actor_role()` with `p_student_id` restricted to NULL or self for authenticated callers. The service writer records actor_id NULL, actor_role 'service', actor_display 'StoryForge system' and may attribute any target student (sweeps act on students who are not present). Both fix `search_path = public, pg_temp` and never accept transcript, story text, audio, token, URL, or free-text content; there is no free-text parameter at all.
- BA, transaction ownership and ordering: audit writes execute on the caller's open transaction and commit or roll back atomically with the mutation they describe. For any flow that also deletes storage objects, the DATABASE transaction (audit + row changes) commits FIRST; object deletion follows post-commit with one immediate retry; a failed object delete is recorded content-free (`object_delete_retried`, counts only) and is guaranteed cleanup by the `storyforge-rec/` 7-day expiry and the weekly reconciliation (Amendment 4). Audit appends are never retried on object-delete retry; state-guard WHERE clauses make every mutation idempotent, and an idempotent no-op writes no duplicate event.
- Forbidden outcomes BOUNDED BY CONSTRUCTION: arbitrary audit injection (vocabulary + payload guard + per-action ownership checks: recording actions require the entity to be the actor's own session, segment, or asset, p_story_id must belong to the actor, denial events carry only the surface/errorCategory shape, and enumerated values are enum-checked); student impersonation (actor derived, target restricted to self on the authenticated path); broad audit insert (no table grant is added; only the two definer functions); broad audit select (no read grant is added by this amendment); content in payloads (whitelist has no content-bearing key and values are length-capped and enum-checked).
- Failure behavior: invalid input SQLSTATE 22023; unauthorized SQLSTATE 42501; db.mjs continues to map 42501 to the 503 fail-closed state for MUTATION audits. DENIAL-BOOKKEEPING EXCEPTION: when a denial audit itself raises 'live identity required' (an ineligible or role-drifted identity being denied), the server drops that bookkeeping event and returns the original clean denial, never a 503.
- Tests: new PostgreSQL cases (grant matrix, vocabulary and payload rejection, impersonation rejection, role checks) in `tests/postgres/voice-audit-lifecycle.test.mjs`; the expected e2e red `tests/e2e/voice-dock-states.spec.mjs:138` must turn green; `tests/unit/recording-store-sideeffects.test.mjs` updates to the two-writer call pattern.

## Amendment 3 · E11 and E13 observability queries · RULED

- VST/VRT: `sf_audit_events` has NO `metadata` column (RP-13; confirmed against the foundation and b1_503 migrations: payload columns are `previous_value`/`new_value` jsonb plus `detail`). The B1-504B Observability Runbook SQL referencing `metadata` is SUPERSEDED for these two queries. Voice error categories already travel in `new_value->>'errorCategory'` at every committed call site (VST, recordings.mjs and flags.mjs).
- BA: two bounded SECURITY DEFINER functions (exact SQL in the companion document), both requiring `sf_has_live_identity(ARRAY['admin'])` internally and granted to `authenticated` (house pattern, matching `sf_submit_story`):
  - `sf_feature_audit_tail(p_limit integer DEFAULT 20)`: rows with entity_type 'feature_flag' and action in (feature_scope_changed, unauthorized_denied), ordered by id DESC, limit clamped 1 to 50; returns id, actor_id, action, previous_value, new_value, created_at. It deliberately EXCLUDES actor_display (a human name) and detail. Raw actor UUIDs ARE returned: they are pseudonymous application identifiers the admin already sees in the flag row's updated_by, and attribution is the point of an audit tail; no name, content, or token is reachable through it.
  - `sf_voice_error_summary()`: last-24-hour counts grouped by `new_value->>'errorCategory'`, restricted to the six permitted categories (mic, upload, transcribe, assembly, save, auth) and the four voice entity types; returns error_category text, count integer, nothing else. Student identifiers are EXCLUDED entirely (counts only), satisfying the flags.mjs content-free contract verbatim.
- BA: one supporting partial index on `sf_audit_events` for the 24-hour category scan (companion document). This is an additive index on an append-only table, not a schema change to any locked column.
- BA: unavailable/error behavior is unchanged: the flags.mjs seams keep returning the privacy-safe 503 codes (`feature_audit_unavailable`, `voice_health_audit_unavailable`) whenever the injected queries are absent or fail. After wiring, `readFeatureAuditTail` and `readVoiceErrorSummary` receive the already-open admin identity client; no service-role cross-student audit read is created anywhere. Bounded-delegation delta registered here: the admin flag surface gains hard size limits of 50 allowlist entries and 20 cohorts (rejected with the existing inline error copy), a product-visible limit introduced so flag audit payloads always satisfy the payload guard; raising it later is a Fable amendment.
- Unrestricted audit-table reads remain forbidden; no new SELECT grant on `sf_audit_events` is issued; the only new read paths are the two admin-gated functions above with fixed shapes, fixed windows, and fixed limits.
- Tests: new PostgreSQL authorization and shape cases (admin allowed; student, mentor, ineligible, and service all denied with 42501; category whitelist enforced); `tests/unit/flags-capability.test.mjs` and `tests/unit/phase1-routes.test.mjs:291-296` flip from 503 expectations to green-path expectations for admin callers.

## Amendment 4 · Private lifecycle and retirement transactions · RULED

- VST/VRT: `storyforge_app` holds full DML on the two recording tables (M1 service policies) but has no path into `sf_story_drafts`, `sf_stories`, or `sf_audio_assets` (RP-13: authenticated SELECT only; service no direct DML), which is exactly why Codex fail-closed `sweepCandidates`, `sweepSession`, E8 retirement, and archive propagation.
- BA: six bounded SECURITY DEFINER functions with EXECUTE granted to `storyforge_app` only (sweep candidates, sweep purge, asset pending candidates, asset mark-verified, asset mark-failed, reference check) plus two granted to `authenticated` (retire, attach), exact SQL in the companion document. The three asset functions exist because RP-13 proves the service role has no path to `sf_audio_assets`, and the post-attach verify/fail lane would otherwise re-create the exact fail-closed gap this amendment resolves; copy retries are time-based at the sweep cadence with a 60-minute pending ceiling, restart-safe with no new column. Highlights:
  - `sf_voice_sweep_candidates(p_limit)` (service): returns session_id, student_id, state, reason for exactly three candidate classes: recording sessions inactive 24 hours WITH the draft-aware exclusion (skip when the student's `sf_story_drafts.updated_at` is within 24 hours); finishing or assembled sessions older than 72 hours; failed sessions older than 24 hours. Limit clamped to 100. No transcript or payload column is returned.
  - `sf_voice_sweep_purge(p_session_id, p_reason)` (service): single transaction, session locked FOR UPDATE; RE-VERIFIES the full candidacy predicate under the lock, including the draft exclusion, so a session the student resumed in the selection window is an idempotent no-op, and an already-swept zero-segment failed session writes NO duplicate audit event; appends `recording_swept` (state, reason, segmentCount only); DELETES segment rows, erasing transcripts and flagged terms from the database; sets the session terminal state; RETURNS the object keys, and the sweep worker additionally deletes the session's whole `storyforge-rec/` prefix (covering any Option A temp artifact). DB commit first; object deletes after, one retry, then the 7-day `storyforge-rec/` expiry and weekly reconciliation are the guaranteed backstops. Exact DB-succeeds-R2-fails behavior: rows and transcripts are already gone (privacy first), orphaned private objects expire within 7 days in a private bucket. Exact R2-succeeds-DB-fails behavior: cannot occur in that order, because no object is deleted before commit.
  - `sf_voice_audio_reference_check(p_object_keys text[])` (service): returns key plus referenced boolean for the weekly reconciliation; reads only object_key and state; the restore guard (reconciliation SUSPENDED after any rung 6/7 event pending founder review) is carried unchanged.
  - `sf_retire_story_audio(p_asset_id)` (authenticated, student-only inside): E8 retirement in one transaction: ownership verified through the student's own identity, asset locked, audit `audio_deleted`, state set to 'retired'; RETURNS the object keys; objects deleted post-commit with retry and the reconciliation backstop (a retired asset's objects are deletable garbage by definition). Playback denies retired instantly, so the student-facing deletion promise does not depend on the object delete.
- BA, story archive propagation (consumes RP-13(d), VRT: story removal is soft archive; no hard-delete path exists): archiving RETAINS the attached, saved audio asset untouched, because archive is reversible and the approved retention ruling ties audio deletion to story DELETION and to the student's explicit E8 delete, neither of which archive is. Open-session propagation is a STRUCTURAL NO-OP in Phase 1: sessions gain `story_id` only inside the terminal attach transaction, so an open session linked to a story cannot exist; the sweep reason `story_archived` is RESERVED with a no-op body, Codex implements nothing for it, and a test asserts the invariant (no open session carries story_id). No hard-delete or 410 branch is invented. Account-closure deletion remains the 30-day support process (FG-1 territory, external).
- BA, retries and idempotency: every lifecycle mutation is guarded by state predicates in its WHERE clause; re-running any of them is a safe no-op; the idempotency key is the session or asset UUID plus the required prior state; retry limits are one immediate object-delete retry, then backstop. No tombstone table is introduced; the two backstops make one unnecessary, honoring the no-new-tables rule.
- BA, `readDraftTitle`: the committed empty-string behavior stands. Draft-derived keywords are an accepted degradation, not a blocker; no service read into private drafts is granted for it.
- Preserved invariants restated as acceptance: student privacy (no new cross-student read anywhere); deletion rights (E8 works under its own identity); saved audio (attached assets never swept; archive retains); rollback safety (rungs unchanged); no orphaned durable audio (reconciliation plus expiry); no unbounded service reads (every service function is fixed-shape, fixed-window, clamped).
- Tests: new PostgreSQL cases for candidate selection (including the draft exclusion and the attached-session exclusion), purge idempotency, retire ownership denial, reference check; `tests/unit/recording-store-sideeffects.test.mjs:94-117` flips to green-path.

## Amendment 5 · Transcription provider contract · RULED

- VRT: B1-505D's provider-contract evidence and Fable's own re-verification of the current official API reference this run agree: the batch endpoint is `POST https://api.openai.com/v1/audio/transcriptions`; accepted models include `gpt-4o-transcribe`, `gpt-4o-mini-transcribe`, and `whisper-1`; the language field is SINGULAR `language` (ISO-639-1); there is NO `keywords` parameter; `prompt` guides style and continues context; gpt-4o models support response_format `json` only, with `include[]=logprobs` available; `stream` exists but not for whisper-1; accepted containers include webm, mp4/m4a, ogg, wav; the request limit is 25 MB.
- BA, superseding the stale identifiers in the B1-504B Transcription Lock (the Lock's thresholds, outcome table, retry taxonomy, privacy rules, and segmentation runtime are NOT superseded): primary model `gpt-4o-transcribe`; fallback model `whisper-1`; realtime contingency (latency-miss branch only, pre-authorized, parked branch) `gpt-4o-transcribe` over the Realtime transcription session API. `gpt-live-transcribe` and plural `languages` are struck. The vocabulary/terminology mechanism is PROMPT COMPOSITION for both drivers: previous-segment context tail (last 200 characters of final text) followed by a bounded vocabulary block from the versioned medical lexicon plus tokenized draft title when available, total prompt capped at 600 characters, composition order fixed. Confidence: primary requests `response_format=json` with `include[]=logprobs` and maps low mean-logprob word spans to flaggedTerms entries with source 'confidence'; whisper-1 uses `response_format=json` with lexicon-only flags (confidence absent, and absent confidence never invents chips, carried law). Full request/response field table, error mapping, retries (30 s abort, one immediate retry on 5xx/timeout, E6 cap 3, backoff 2 s then 8 s, at most two in-flight calls per session with the committed single-active FIFO standing, transcribed-state idempotency), failover conditions (unchanged from the committed adapter, VST), no-provider behavior (unchanged provider-off truthfulness), and environment variables are in the companion document. `validateConfig` is amended to accept exactly `none` or `openai`, with `openai` requiring `STORYFORGE_OPENAI_API_KEY`; production remains `none` until the runbook's activation step flips it.
- BA, bake-off: the committed deterministic scorer (hash-pinned `9f5b9981...`/`9b269ab0...`) IS the binding normalization and aggregation; its committed behavior is approved as-is and `metricsUsableForActivation` flips only when the inputs are the consented human corpus plus real provider runs. Candidate configurations are exactly three: C1 primary with full prompt composition; C2 primary with context tail only (this pair is the keywords-enrichment comparison the Lock's accuracy-miss branch requires); C3 whisper-1 with full prompt composition folded into the prompt (fallback baseline). The Lock's threshold table and SINGLE OUTCOME TABLE govern unchanged. `gpt-4o-mini-transcribe` is recorded as available but is NOT a candidate; adding any candidate or vendor remains FG-3.
- REP, the RP-7 model probe (non-logging, StoryForge-scoped key only, exact script already prepared in the Codex handoff Section 10.4) with `STORYFORGE_MODEL_IDS_CSV="gpt-4o-transcribe,whisper-1"`. Deterministic and EXHAUSTIVE outcomes: both true, drivers armed and the bake-off may run when the corpus exists; primary false and fallback true, fallback-only posture with the Lock's accuracy rule (whisper-1 may become launch primary only by passing the accuracy gates) and FG-3 prepared; primary true and fallback false, SB to Fable (the wired-fallback mandate is unsatisfiable and the key's model scope goes back for review); both false, SB to Fable; HTTP 401/403, credential lane stop (CREDENTIAL-2). Confidence-span semantics and prompt truncation are pinned deterministically in the companion document Section 4 (word-level spans by token-offset mapping, lexicon-wins dedupe, drop vocabulary from the end, tail always survives). Codex MAY implement both drivers and their unit tests BEFORE the probe (the code is inert under provider-off and the config guard); ACTIVATION remains blocked behind the probe, the corpus, the bake-off decision record, and the S14 gates. Mock transcription remains forbidden in production paths; test-double drivers inside the test suite are not production mocks.
- Privacy: RP-12 posture evidence (VRT: API inputs not used for training by default; transcription endpoint ZDR-eligible) is compatible; MissionMed's actual ZDR/BAA contractual state remains an EXTERNAL FOUNDER item (CREDENTIAL-2), required before S14, not before implementation.

## Amendment 6 · RP-8 assembly and the E4/E7 finishing transaction · RULED, WITH THE OPTION SELECTION STILL BLOCKED

- VRT: RP-8 is `BLOCKED_DOCKER_CAPACITY_NO_OPTION_SELECTED`; the host-only supplemental timings (0.11 s WebM, 0.12 s M4A with DTS warnings) are recorded context and select nothing. SB: the Option A vs Option B selection remains blocked until the healthy-container probe runs; the probe definition (R2 spec Section 4, local Nixpacks container, 40-segment 10-minute fixture, 60 s gate, Chrome and Safari playback) is unchanged and remains the exact REP. The mandatory action for each result is unchanged from the R2 spec's binding table: first row locks Option A, second row locks Option B, and Codex implements exactly the locked one; both executors may be BUILT now behind the existing injected boundary, and the probe outcome only selects which is WIRED.
- BA, option-independent protocol (this resolves the finishing-session contradiction and is implementable immediately; exact transaction SQL and sequence in the companion document):
  1. E4 (Save pressed): transaction moves recording to finishing (committed behavior stands) and triggers asynchronous assembly. Assembly produces artifacts only under the session's `storyforge-rec/` prefix (Option A: one concatenated `assembled.{ext}` object; Option B: validation only, segments are the artifacts). `assembled` now means artifact-ready with `assembled_asset_id` NULL; the committed `markAssembled` drops its asset-id parameter, because the DURABLE asset row cannot exist before the story exists (`sf_audio_assets.story_id` is NOT NULL, VST) and no fake asset may be created.
  2. E7 (voice save): the handler FIRST reads the owned session; `attached` returns the already-linked story (this is the idempotency mechanism; a lost response plus retry is safe); `finishing` returns 409 `voice_assembly_pending`; `assembled` runs ONE database transaction on the student's identity connection: the existing `sf_create_story_v5` (story, revisions, draft semantics untouched) then the new `sf_attach_recording(...)` SECURITY DEFINER function (student-only, ownership-locked), which creates the `sf_audio_assets` row in state 'pending' with a server-computed, extension-free, OPTION-NEUTRAL permanent key prefix, transitions the session assembled to attached, links `assembled_asset_id` and `story_id`, inserts the immutable audio original (transcript and asset together, exactly once; `sf_create_story_v5` deliberately skips originals for audio captures), and appends `audio_attached`. Any failure rolls the WHOLE transaction back; a concurrent-duplicate 42501 rolls back, re-reads, and returns the winner's story. No story without its audio linkage, no partial attachment, no story claiming audio it does not have.
  3. Post-commit R2 phase (service): copy the temp artifact or segments to the permanent key shape (Option A: `key.ext` single object; Option B: `key/seg-NNNNN.ext` per segment; both prefix-covered by the reference check and by E8 prefix deletion), HEAD-verify, then finalize through the bounded service functions `sf_voice_asset_mark_verified` / `sf_voice_asset_mark_failed`, which also purge the session's segment rows in the SAME transaction so a completed-lifecycle session never retains transcripts. Retries are time-based at the sweep cadence; a pending asset reaching 60 minutes is finalized failed with audit. Until 'verified', playback returns the truthful unavailable state and never a broken player; a failed asset's story keeps its transcript text with the truthful audio-unavailable state.
  4. E7 arriving while the session is still finishing returns 409 `voice_assembly_pending` with retryAfterMs 2000; the client polls E3 and retries for up to 90 seconds, then offers the student a truthful choice: keep waiting, or save the typed story now WITHOUT audio through the EXISTING explicit discard flow (E5 cancel with its confirm dialog). RACE RULE: if that cancel is refused because assembly completed meanwhile, the client retries E7 once, the save completes WITH audio, and E8 delete remains available; nothing is silently lost and no session is left trapped. Assembly failure follows the committed `markAssemblyFailed` path (state failed, audit, category assembly), the client shows the new fixed string below, and the failed session is swept on the standard schedule. The 72-hour finishing/assembled sweep remains the backstop guaranteeing no recording is trapped indefinitely.
  5. Typed-only story save NEVER touches E4/E7 voice steps and never waits on assembly (committed behavior, preserved).
  6. Idempotency: the E7 pre-read plus state guards; the client retry token IS the session UUID; the attach function's own attached-branch return is the concurrency backstop.
- BA, one new PA-immutable error string (authored here under the product authority's bounded-delegation power; the fixed seven carry unchanged): save-time assembly failure: "We couldn't attach your audio this time. Every word is safe in your story text. You can save your story now, and you can record again anytime." String SHA-256 `669fc79d5bc8b51689f4112c9032d7d440c242b86a100119288f75b606577ff8`. THIS AMENDMENT IS the delta-ledger registration for that string (the conformance matrix document itself stays untouched; its ledger law is satisfied by this recorded authority entry, and the string is an error-state addition, not a new surface).
- SB: wiring of Option A vs Option B, and therefore end-to-end voice-save activation, remains blocked on the healthy Docker/Nixpacks RP-8 run (EXTERNAL-1). Codex has no authority to select the option from the host-only timings.

## Hashes, tests, and release gates (consolidated)

Files changed by this amendment: `infra/postgres/migrations/20260729000100_b1_506_voice_recording_sessions.sql` (amended in place, new SHA `6f6a3340...`); new `infra/postgres/migrations/20260729010000_b1_506a_voice_audit_lifecycle.sql` and its `_rollback.sql` (hashes recorded by Codex at commit and pinned into the runner); `scripts/apply-production-migrations.sh` (three amended-M1 pin locations, plus the new migration's hash, ledger rows, and a new confirm token `B1-506A-APPLY-THREE-MIGRATIONS`); `server/db.mjs` (two-writer audit calls plus the denial-bookkeeping exception); `server/flags.mjs` (wiring of the two approved queries on the open client; allowlist and cohort size bounds in validateMutation); `server/recordings.mjs` (lifecycle store functions replacing the 503 seams, DB-commit-first ordering, prefix-wide temp cleanup, markAssembled signature, E7 pre-read flow, attach and asset finalization, 409 pending path, SQLSTATE-to-HTTP mapping); `server/transcription/openai-gpt-4o-transcribe.mjs` and `openai-whisper1.mjs` (new drivers) plus `adapter.mjs` provider branch and `config.mjs` validateConfig; `public/app.js` (E7 pending retry loop with the race rule, the one new error string); the playback/presign path in `server/recordings.mjs`/`server/storage.mjs` (the binding playback key rule for legacy full keys vs new stems); tests as listed per amendment. Nothing else changes; the canonical product artifacts and all B1-504B documents are untouched. Codex records the PRE-EDIT hash of `scripts/apply-production-migrations.sh` in the implementation handoff alongside the post-edit hash, so the pin change is itself evidenced.

Expected post-implementation local state: PostgreSQL 142/142 for the pre-amendment case set with ZERO failures overall (new amendment cases add beyond 142; the binding rule is zero failures and the four M1 reds plus one e2e red flipped green); browser E2E 34/34 plus any added specs green; conformance exactly 72/72 (no visible-surface change except the authored error string, which is an error-state addition registered in the delta ledger, not a new surface); unit all green; release, API, integration, and migration-preflight entrypoints STOP conditions cleared for M1 (their other gates unchanged); secret scan and audit clean; Git clean.

Gates that become unblocked by this amendment alone: the M1 release stop across all four entrypoints; the audit-dependent lanes (cancel, delete, sweeps, flag audit, E11, E13); the lifecycle lanes; the provider drivers and bake-off harness readiness; the E4/E7 protocol implementation. Gates that remain external: RP-8 option selection (Docker health, EXTERNAL-1); RP-7 key and model probe plus contractual posture (CREDENTIAL-2); Cloudflare/R2 evidence and provisioning (CREDENTIAL-1); fresh production identity, backups, and deployment sessions (CREDENTIAL-3); FG-1, two-account activation, corpus, roster, S13/S14 (FOUNDER-1..3); B1-505 for S18+ (scope note, unchanged).

Codex retains ZERO architectural discretion under this amendment: every ruling above is exact, every remaining unknown is carried by a named probe with a deterministic outcome table, and the two B1-504B verbatim rules remain in force word for word, including: "Codex may prove a fact. Codex may not convert an unresolved fact into an architectural choice."

## Independent review closure record

Two fresh-context reviewers ran against the draft package. Reviewer 1 (PostgreSQL, RLS, audit, lifecycle, privacy) independently reproduced the Amendment 1 hash arithmetic and the four-reds-turn-green claim, verified the audit schema and disjoint-grant mechanism, then returned 4 P0 and 5 P1 findings. Reviewer 2 (provider contract, assembly, E4/E7, implementability, scope) independently reproduced the hashes and counts and the provider contract, then returned 5 P0 and 7 P1 findings. Every P0 and P1 was closed with a text or SQL change in this package before delivery, the largest being: `latencyMs` added to the payload whitelist with value enums for every enumerated key; three bounded service asset functions added (pending candidates, mark-verified, mark-failed) closing the sf_audio_assets gap with time-based restart-safe retries and same-transaction segment purges; the permanent object key made extension-free and option-neutral so the reference check and prefix deletion cover both assembly options and reconciliation can never delete saved audio; archive propagation ruled a structural no-op with a reserved reason and an asserting test; sweep purge now re-verifies full candidacy under the lock and is audit-idempotent for already-swept sessions; the E7 idempotency mechanism made explicit (pre-read, 409, race-loser re-read) and the discard-fallback race ruled (refused cancel retries E7 once, saving WITH audio); the denial-bookkeeping 42501 exception; per-action entity-ownership checks in the authenticated writer with the injection claim downgraded to bounded-by-construction; allowlist/cohort size bounds; the RP-7 outcome table completed with the primary-true/fallback-false row; word-level confidence spans and prompt truncation pinned; the concurrency description corrected to the committed single-active FIFO; capabilities and driver-filename supersession stated; the checksum_sha256 column named exactly; attach SQLSTATE-to-HTTP mapping fixed; the reference check clamped; the error-summary predicate made index-usable; the new string's ledger registration recorded with its SHA-256. P2 items were incorporated where they were corrections and recorded here where they were observations (the runner-script pin is verified by Codex's own preflight hash gate at implementation time). Reviewer 1's second pass verified all fifteen first-pass closures as genuinely closed with valid SQL and surfaced one residual P1 (the 50-entry allowlist could not fit the 2048-character payload cap) plus four P2s; all were fixed in this text (payload cap raised to 4096 with the arithmetic documented; retire-while-pending now purges the linked session's segment rows; the binding content-type-to-extension map pinned for copy and playback; segment_count zeroed in every purge path; this record updated to the actual final verdicts). Recorded deliberate divergence: the voice asset finalizers do not set `sf_audio_assets.immutable_at` (the column exists per b1_503); nothing in the voice lane reads it and `verified_at` is the voice marker. Reviewer 2's second pass then verified all eighteen first-pass closures and surfaced one new P0 introduced by the revision itself (zeroing segment_count at verify would have destroyed the Option B playback manifest) plus two P1s (the playback key-derivation rule for legacy full keys vs new stems was unassigned; the closure record pre-claimed verdicts) and eight P2s; all were fixed in this text (segment_count RETAINED at verify with the Option B enumeration pinned; the binding playback key rule and the playback module added to the files-changed list; duration_ms populated at verify; the six-function count corrected; the combined handoff's stale phrases corrected; the truncation drop order disambiguated; the two superseded Amendment 5 phrases aligned; the 50/20 caps registered as a bounded-delegation delta; the test inventory extended; the manifest generated at packaging). Final confirmation verdicts, recorded after the fixes were applied and re-reviewed: Reviewer 1 SOUND; Reviewer 2 SOUND.

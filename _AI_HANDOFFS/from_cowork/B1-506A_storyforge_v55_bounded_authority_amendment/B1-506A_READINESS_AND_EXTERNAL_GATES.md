# B1-506A · Readiness and External Gates

## Locally implementable NOW (this amendment, no external input)

Amendment 1 M1 predicates and runner pins; Amendment 2 audit writers and every audit-dependent lane (cancel, delete, denials, flag audit); Amendment 3 E11/E13 queries and wiring; Amendment 4 lifecycle, sweeps, retirement, archive propagation, reconciliation query; Amendment 5 both provider drivers, adapter branch, config validation (inert, provider-off); Amendment 6 E4/E7 protocol, attach transaction, 409 pending path, post-commit copy/verify machinery, BOTH assembly executors unwired, the new error string. Expected local state afterward: PostgreSQL 142/142 pre-amendment cases plus new cases, zero failures; e2e 34/34 plus new spec; conformance 72/72; unit green; release/API/preflight entrypoints past their M1 stop; secret scan and audit clean.

## Remains blocked, by owner

- FOUNDER: FG-1 retention/consent/wind-down ruling (blocks the ruled copy and S14, not implementation); two-account confirmation and the S12a activation; corpus readers, verifier, custody (blocks the bake-off); roster and operations items; S13 signature; any WordPress access-policy or flag change.
- CREDENTIALS: Cloudflare read-only session (RP-4/RP-6) then the separately authorized R2 provisioning (S5); StoryForge-scoped OpenAI key plus the RP-7 model probe (`gpt-4o-transcribe,whisper-1`) and the MissionMed ZDR/BAA contractual evidence (before S14); fresh time-bounded Railway/Kinsta/WordPress/PostgreSQL sessions and new S4 backups with restore rehearsal (before S7).
- DOCKER/RP-8: a healthy container engine restored by an authorized human workflow (no destructive repair by any run), then the binding Nixpacks probe selects Option A or Option B per the R2 spec table; the selected executor is then WIRED (mechanical).
- PRODUCTION PREFLIGHT: fresh RP-3/4/5/6/7/10/12/13, then S7 to S11 per the runbook, then S12a; the bake-off decision record plus FG-1 copy plus S13/S15/P4X gate S14 exactly as B1-505C bound them.

## Expected remaining engineering time after this amendment is implemented

Consistent with the Codex handoff Section 13 and unchanged by this amendment: 10 to 16 active hours for amendment implementation and local green reruns; 8 to 12 active hours for probes, backups, and S7 to S11 once credentials exist; total to founder-only S12a roughly 18 to 28 active engineering hours, excluding founder, corpus, credential, and observation time. A new contradiction, live drift, failed restore, threshold miss, or privacy defect reopens the applicable stop.

# B1-506A · Combined Handoff · Bounded Fable Authority Amendment

READY FOR CODEX AMENDMENT IMPLEMENTATION. Issued by Fable under founder directive B1-506A against Codex handoff commit `411b7aee...` (implementation `de1828cc...`), branch `codex/b1-503-storyforge-product-recovery`. This folder is the complete amendment: rulings (`B1-506A_FABLE_AUTHORITY_AMENDMENT.md`), literal SQL and contracts (`B1-506A_EXECUTABLE_SQL_AND_CONTRACTS.md`, governing text), the paste-ready implementation prompt (`B1-506A_CODEX_IMPLEMENTATION_PROMPT.md`), external gates (`B1-506A_READINESS_AND_EXTERNAL_GATES.md`), and `MANIFEST.sha256` pinning every amendment file plus the immutable inputs.

The six rulings in one paragraph each:

1. M1 STUDENT-ONLY RLS: the four requested `sf_has_live_identity(ARRAY['student'])` substitutions are APPROVED VERBATIM; Fable independently reproduced the amended hash `6f6a3340...` from the on-device file; rollback and M2 unchanged; three runner pins updated; no other grant or policy changes; mentors, admins, and the service role gain nothing; the four red denial tests are the acceptance gate.
2. BOUNDED AUDIT WRITER: two new SECURITY DEFINER functions with DISJOINT execute grants (authenticated writer with actor derivation, self-only targeting, role checks; service writer with actor 'service', system surface only), a fixed partitioned event vocabulary, a whitelist payload guard with no content-bearing field, fixed search_path, in-transaction semantics, and DB-commit-first ordering before any object delete. `sf_append_audit` stays ungranted. Arbitrary injection, impersonation, broad insert, broad select, and content in payloads are bounded by construction, with per-action entity-ownership checks. The one expected e2e red turns green.
3. E11/E13: the nonexistent `metadata` column authority is superseded by two admin-gated bounded functions: `sf_feature_audit_tail` (feature-flag actions only, id DESC, limit clamped 50, actor_display excluded, raw pseudonymous UUIDs justified) and `sf_voice_error_summary` (24-hour counts by the six permitted categories, counts only, no identifiers), plus one partial index. The 503 fail-closed seams remain for absence or failure. No broad audit read exists anywhere.
4. LIFECYCLE: six service-granted bounded functions (sweep candidates with the draft-aware exclusion; sweep purge that re-verifies candidacy under the lock, erases transcripts with the rows, and is audit-idempotent; asset pending candidates, mark-verified, and mark-failed closing the service's sf_audio_assets gap with time-based restart-safe retries; the reconciliation reference check, clamped) plus the student-granted retire and attach functions. Archive ruling: attached audio is RETAINED (soft archive is reversible; deletion rights stay with E8 and account closure) and open-session propagation is a structural no-op proven by test, because sessions gain story_id only at terminal attach. Compensation is the 7-day `storyforge-rec/` expiry plus weekly reconciliation; no new tables; every mutation idempotent by state guard.
5. PROVIDER CONTRACT: primary `gpt-4o-transcribe`, fallback `whisper-1`, endpoint `/v1/audio/transcriptions`, SINGULAR `language`, NO keywords parameter (vocabulary folds into a 600-character capped prompt composition), `include[]=logprobs` confidence on the primary with the fixed low-confidence threshold, json format, the committed retry/failover/no-provider behavior unchanged, `validateConfig` accepting exactly none or openai, three exact bake-off candidates, the committed scorer as binding normalization, the Lock's thresholds and single outcome table unchanged, and the RP-7 probe with a deterministic five-row outcome table. Drivers are implementable NOW and inert; activation stays gated. Realtime remains the pre-authorized latency-miss contingency on a parked branch.
6. RP-8 AND E4/E7: the Option A/B selection REMAINS BLOCKED on the healthy-container probe (host-only timings select nothing); everything else is ruled option-independent: assembled means artifact-ready with a NULL asset id; the durable asset row is born inside the E7 identity transaction (`sf_create_story_v5` + `sf_attach_recording`, which also inserts the audio original exactly once) under an extension-free OPTION-NEUTRAL key prefix that both assembly shapes, the reference check, and E8 prefix deletion all share; E7 idempotency is an explicit pre-read (attached returns the linked story; the race loser re-reads); post-commit copy/verify/finalize runs through the bounded asset functions with truthful pending playback and time-based retries; 409 `voice_assembly_pending` with a 90-second client window falls back to the existing explicit discard flow, and a cancel refused because assembly completed retries E7 once and saves WITH audio; the 72-hour sweep is the trap-proof backstop; one new PA-immutable failure string (SHA `669fc79d...`, registered by this amendment). No partial story, no fake asset, no trapped recording, no silent loss.

Codex retains zero architectural discretion; the remaining unknowns are carried by RP-7, RP-8, the credential lanes, and the founder gates listed in the readiness document, each with a deterministic outcome table. Verification: two independent fresh-context reviewers (PostgreSQL/RLS/audit/lifecycle/privacy; provider/assembly/implementability/scope) ran against this package; every P0 and P1 finding was closed before delivery, with the closure record appended to the amendment document.

Next actions in order: (1) paste `B1-506A_CODEX_IMPLEMENTATION_PROMPT.md` into Codex GPT-5.6 Sol Ultra; (2) in parallel, the founder-side external gates proceed (Docker repair for RP-8, scoped key for RP-7, Cloudflare session, FG-1, corpus, roster); (3) after the implementation handoff is green, production preflight resumes at the runbook with fresh evidence per B1-505C.

Codex may implement this amendment. Codex may not reinterpret it.

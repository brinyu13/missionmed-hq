# Y1-Y2-CAM-V6-3492 — CODEX RETURN CONTRACT

This run deployed nothing, touched no LemonSlice, created no provider sessions, modified no production.

## 1. State Codex receives

Design convergence complete: MISSIONMED PERFORMANCE ENGINE system frozen (`_FINAL_DESIGN_SYSTEM.md`); component ledger shows exactly **two open Founder decisions** (Gesture Field GF-B/C/D; Mentor MENT-A2/C/D) — everything else LOCKED or REFINED-LOCKED. Normative reference implementation: `Y1-Y2-CAM-V6-3492_IVPREP_FINAL_NORTHSTAR.html` (integrated app flow, functional navigation, scenario deck as QA fixture). Frontend build spec: `_CLAUDE_CODE_FRONTEND_BUILD_CONTRACT.md`. Telemetry spec: `_DELIVERY_INTELLIGENCE_WIRING_CONTRACT.md`. Avatar spec: `_AVATAR_STAGE_INTEGRATION_CONTRACT.md`.

## 2. Codex responsibilities (unchanged from 3490 where not superseded)

1. **Adapters:** map accepted primitives to the frame contract per the wiring table's raw-input column; fail-closed mapping lives in the adapter. Support-class honesty preserved (pitch/cadence/phase/semantics = NEW; ship UNAVAILABLE until real).
2. **Dr Kelly:** implement the session-layer events in the Avatar contract §4; run the human-gated QA checklist §5; fix natural initiation; verify the fallback ladder downgrades without losing session, recording, or Delivery Intelligence.
3. **Persistence & privacy:** derived lanes + consented recordings only; raw frames/audio/landmarks never persisted; mentor marks attributed; mentor data scope separate from student scope at the API level (privacy law is architectural).
4. **Entitlements:** tier gates server-side; avatar tier flagged HUMAN QA REQUIRED until §5 passes.
5. **CI:** 3472 §13 string-lint over UI strings, keys, logs, exports; scenario-deck acceptance runs (S1–S6 + ticket scenarios) against the integrated screens.

## 3. Hard blockers (do not bypass)

No autonomous LemonSlice sessions — Founder-initiated only. No production deploy before the QA loop passes ×5. No student-facing composite before ADM threshold/weight validation is recorded. No phase-aware coaching claims before the phase contract exists. Training-mix customization must never write to canonical profile records.

## 4. Return payload

Updated `CURRENT_STATE.json` (3483 convention): per-detector adapter coverage, Kelly QA checklist status per step, fallback matrix results, entitlement wiring, lint status, deltas vs this contract — plus the two Founder decisions (GF, MENT) once returned, which close the design ledger entirely.

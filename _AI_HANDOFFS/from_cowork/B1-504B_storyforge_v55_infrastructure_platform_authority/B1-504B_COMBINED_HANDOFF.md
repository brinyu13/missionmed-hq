# B1-504B · StoryForge V5.5 Phase 1 · Infrastructure, Platform, and Execution Authority · Combined Handoff

Date: 2026-07-29 (evidence verified 2026-07-28) · Author: Claude Cowork (Fable 5, xhigh) · VERDICT: NOT READY FOR CODEX IMPLEMENTATION (discovery-only packet issued; execution MegaRun deliberately withheld until evidence closure). This file is complete on its own: every material fact, decision, contract, gate, command, acceptance criterion, deployment step, rollback procedure, and Codex instruction in the package appears here. Sibling documents add depth, SQL, and tables in full fidelity.

Labels: VERIFIED PRODUCTION TRUTH (VPT) · VERIFIED SOURCE TRUTH (VST) · PRODUCT AUTHORITY (PA) · ARCHITECTURE AUTHORITY (AA) · RECOMMENDATION (REC) · REQUIRED PROBE (RP-n) · FOUNDER GATE (FG-n) · BLOCKER · SUPERSEDED · UNKNOWN. Ladder: L0 unknown, L1 located, L2 source/config verified, L3 deployed dormant, L4 tested in controlled environment, L5 proven in production. Code presence proves L1/L2 only; nothing upgrades without evidence.

## 1. Authority chain and artifacts (VPT/VST hashes verified on the Mac)

Founder -> Fable authority -> Codex execution -> evidence -> Fable amendment. Codex may prove facts; Codex may not convert unresolved facts into architectural choices; contradictions stop the lane and return to Fable (both rules verbatim in every Codex packet).
Artifacts: canonical V5 parent `3ac2871ff286552abe89a785ff43967df3315922e3718f67a136b83db1ba8db1`; V5.5 prototype (product law) `0df61b561b2a6dfa3e132255381bef05028fd384597adfc8969929c646129c90`; r2 retention-copy revision `95104069500fdca8b92dbe81d5ce9ee7701a5f97400e1d1653414d19d4f13c0b` (two lines only; ships under FG-1 approval); B1-504A package (hash-manifested) remains product/architecture authority where not superseded here; `B1-504A_CODEX_5.6_SOL_ULTRA_MEGARUN.md` is SUPERSEDED as a runnable mission. Approved retention copy (PA, immutable): "Original audio · kept with your story · heard by your mentor only when you submit · delete anytime". Forbidden copy: "private to you". Bounded-delegation strings (delete-audio control, confirm dialog, first-recording consent notice incl. "processed by MissionMed's transcription service" and the avoid-patient-identifiers reminder) are fixed in the B1-504A Authority Lock and immutable.
Conformance law (verbatim): the prototype is product authority, not a production bundle; implement its approved behavior faithfully inside the existing live StoryForge system; do not reinterpret, simplify, redesign, or deploy the HTML as a replacement application. "Functionally similar / technically complete / close enough" are insufficient; release requires product-conformance validation against the prototype artifact. Exactly ONE approved visual delta exists: the 390 px bottom-nav clip fix, shipped unflagged.

## 2. Topology (locked; probes close the marked gaps)

Path: browser -> `https://missionmedinstitute.com/storyforge/` (VPT) -> Cloudflare zone proxy (VPT: DYNAMIC/BYPASS cache posture; HTML never edge-cached; hashed assets immutable max-age) -> Kinsta WordPress origin serving the StoryForge static runtime via the isolated MU route (`infra/wordpress/missionmed-storyforge-route.php` + runtime; immutable-release pointer; VST+VPT) -> WP SSO plugin token exchange `/wp-json/missionmed/v1/storyforge/token` (VST, VPT) -> Railway API-only origin (`railway.json`: NIXPACKS, `npm run build:api`, start `node server/app.mjs`, healthcheck `/healthz`; VPT deployment `fa7ad084-4dae-4039-a154-2250a407d95e`) -> isolated Railway PostgreSQL 18 (VPT deployment `f5c7179e-b805-4e82-b080-d2349a0a47cf`; 25 RLS tables; roles `anon`/`authenticated`/`storyforge_app`, VST bootstrap) -> R2 (seam VST at L2; production state UNKNOWN, RP-6) -> transcription adapter (to build) -> OpenAI (account VST via hq; StoryForge key UNKNOWN, RP-7).
Supabase RESOLVED (AA): no Supabase component participates in StoryForge; root `supabase/` belongs to other systems and is out of bounds (VST AGENTS.md + B1-502M pinning + B1-503 receipts).
Edge worker `missionmed-storyforge-v5` (`infra/edge/`, VST): dormant B1-502 candidate; NOT in Phase 1; RP-4 must prove no `/storyforge*` worker route is active; active route = BLOCKER to Fable. No DNS/zone/CDN changes are authorized in Phase 1.
Secrets ownership: WP HS256 signing secret (>= 32 chars enforced) in WordPress; API verifies via `STORYFORGE_JWT_SECRET` shared secret (HS256 path is production truth for the pilot; no auth migration in Phase 1); `STORYFORGE_R2_*` and `STORYFORGE_OPENAI_API_KEY` live ONLY on the Railway API service; hq keys are separate; rotations independent.
Backups discipline (VPT pattern): Kinsta recovery points, PG dump + restore rehearsal, Railway variable export, WP plugin + settings option export.
Staging: UNKNOWN (RP-9); fallback AA: local harness `scripts/run-local.sh` (loopback dev-auth fixtures incl. cross-student and unassigned-mentor personas, VST) + staging bucket.

## 3. Identity, WordPress, Matrix (VST line-verified)

Entitlement (`missionmed-storyforge-sso.php`): `storyforge_enabled` master switch (activation forces false, VST README); `allowed_user_ids` allowlist; `app_role_overrides` (student/mentor/admin); `allowed_roles`; cohort gate over `allowed_cohorts` vs user meta `_missionmed_storyforge_cohort` (fallback `_mmed_cohort`, filter hook), deny = `WP_Error('cohort_not_enabled', 403)`; rate limit 20/60 s. Matrix entry enqueues `assets/matrix-launch.js` only after the server-side check; plugin deactivation restores legacy `#storyforge` without touching `missionmed-hub`.
JWT (HS256, `mmsf_issue_jwt`): iss (settings), aud `storyforge`, sub = StoryForge UUID (`mmsf_storyforge_user_id`; unmapped -> 503 `storyforge_identity_unmapped`), iat/nbf(now-2)/exp (TTL clamp 60..300, default 120, production 60 VPT), jti UUID, wp_user_id, name, app_role, `storyforge_eligible: true`, and COHORT WHEN NON-EMPTY (VST: the cohort claim already exists in minted tokens).
API verification (`server/auth.mjs`, jose): pinned iss/aud, clockTolerance 5 s, required sub/iat/exp/jti, role in {student, mentor, admin}, `storyforge_eligible === true`, UUID-shape sub/jti, positive wp_user_id; frozen identity {sub, role, eligible, wpUserId, name, issuer}. THE ONLY AUTH CHANGE IN PHASE 1 (AA): surface `cohort: String(claims.cohort || '')` on that frozen identity. R-8 admin mapping, two-account rule: the founder's pilot account KEEPS its `student` override (it is the tester); a SEPARATE founder-controlled account receives the `admin` override (WP SETTINGS change, backup-first, S12), exempt from the B1-505 stop scope because it grants no student access. "Access-policy change" under that stop scope = changes to `allowed_user_ids` membership, `allowed_roles`, `allowed_cohorts`.
Browser posture: Bearer tokens in memory (never localStorage); CORS pinned to `STORYFORGE_ALLOWED_ORIGINS`; no cookies on the API; no new CSP in Phase 1 (hardening = backlog); iframe check in RP-10 (if any iframe embeds the app, it must carry `allow="microphone"`).

## 4. Database authority (full SQL in the DB spec; summary here is binding)

All 25 existing `sf_*` tables enumerated and RETAINED UNCHANGED, including: sf_users (durable identity; JWT sub = sf_users.id), sf_mentor_assignments, sf_stories (status/row_version/capture_type/themes/uses/birds/positions), sf_story_originals (immutable original), sf_story_revisions (version history), sf_story_drafts (+`sf_save_story_draft(jsonb,bigint)`), sf_story_reflections, sf_story_craft, sf_feedback, sf_questions, sf_story_questions, sf_question_preferences, sf_pair_followups, sf_question_coaching_notes, sf_next_questions, sf_use_suggestions, sf_coaching_sessions(+items), sf_notifications, sf_import_batches(+rows), sf_audit_events (append-only, trigger-protected), sf_audio_assets (pending/uploaded/verified/failed/retired), sf_ai_suggestions (V5 AI, env-flag off, untouched), sf_workshops. RLS house primitives: `sf_has_live_identity()`, `sf_actor_id()`, `sf_actor_role()`, `sf_is_assigned()` (VST; the only primitives new policies may use).
New migrations (exact SQL in the DB spec; additive only; guarded runner `scripts/apply-production-migrations.sh`): M1 `sf_recording_sessions` (state machine recording/finishing/assembled/attached/cancelled/failed; one-active-session partial unique index; caps 200 segments / ~21 min ms bound; last_activity_at for sweeps) + `sf_recording_segments` (UNIQUE(session,seq); object_key UNIQUE; MIME allowlist; <= 5 MB; transcribe_state received/transcribing/transcribed/transcribe_failed; transcript; flagged_terms jsonb; retry <= 3), both ENABLE + FORCE RLS, student-own-rows policies, NO mentor/admin policies (recordings are pre-save private), grants SELECT/INSERT/UPDATE to authenticated PLUS explicit storyforge_app service grants (ALL) with USING(true) service policies for the transcription worker, sweeps, and E13; cancel and sweeps PURGE segment rows (transcripts erased from the database). M2 `sf_feature_flags` (key PK; scope off/allowlist/cohort/eligible_all, the `founder` scope value is REMOVED: the founder stage is allowlist containing exactly the pilot UUID; allowlist uuid[]; cohorts text[]; updated_by FK sf_users; admin-only RLS plus explicit storyforge_app service-read policy; seeded `voice_capture`=`off` by deterministic earliest-user select with a DO-block existence assertion; founder-UUID injection as documented runner override when RP-10 confirms the mapping). Validation queries and reversal companions specified; reversal only with founder authorization + fresh backup, never to disable voice. No SECURITY DEFINER additions for flags; capability computed app-layer. E7's session-ownership check is explicit in-transaction (SECURITY DEFINER paths bypass RLS; carried P0 rule).

## 5. Storage and lifecycle (binding)

Buckets `missionmed-storyforge-audio-prod` / `-staging` (create only under implementation authority; RP-6 first); scoped token, no public access, no r2.dev, no CDN, presigned-only; CORS = allowed origins, GET (playback) + PUT (legacy presign path only; segments travel through the API), max-age 3600. Keys: assets `storyforge-audio/{studentUuid}/{storyUuid}/{assetUuid}.{ext}` (existing, retained); segments `storyforge-rec/{studentUuid}/{recordingUuid}/seg-{seq:05d}.{ext}` (always transient: under Option B the segments are COPIED at attach to permanent `storyforge-audio/.../{assetUuid}/seg-*` keys and the rec/ originals die with the session). Caps: 50 MB asset, 5 MB segment, 20 min recording (countdown from 18:00; server hard stop), 200 segments, one active session, `STORYFORGE_VOICE_DAILY_MINUTES=60`. Upload: segments POST to the API (multipart), API writes R2; client never holds segment URLs; legacy presign+PUT stays deployed, UNUSED by voice, subordinated to the flag. Playback: existing presigned GET, TTL 300 s (clamp 60..900), every issuance re-verifies identity + ownership/mentor-visibility + state, outside the voice flag. Deletion: E8 outside the flag; object delete + retire + audit; story delete cascades; HEAD 404 evidence. Assembly DECISION TABLE (RP-8): ffmpeg feasible and 10-min assembly <= 60 s and dual-browser playback -> OPTION A single-object concat remux (async, registered via `sf_begin_audio_asset`/`sf_confirm_audio_asset`); else OPTION B ordered-segment manifest playback with one continuous control; no third path. Sweeps: 10-minute interval task (`STORYFORGE_SWEEPS=on` default) through the privileged service path; cancel = immediate object deletion AND segment-row purge (transcripts erased); abandoned (24 h no activity) identical + `failed` + audit; stuck finishing/assembled 72 h; bucket lifecycle backstop 7 days on `storyforge-rec/`; no expiration on assets; weekly reconciliation deletes unreferenced storyforge-audio objects older than 7 days, SUSPENDED after any rung 6/7 restore pending founder review. Review-idle guard: the client keeps polling (E3) through client-side review so an open review never goes sweep-eligible; the sweep additionally excludes sessions whose draft was updated within 24 h. Encryption: R2 at rest + TLS; no envelope KMS in Phase 1. Audio single-copy in R2 (conscious trade-off; transcripts DB-backed); retention: student delete-anytime, story-deletion cascade, account closure 30 days, revocation deletes nothing.

## 6. Transcription (binding)

Adapter `server/transcription/` (adapter.mjs, openai-gpt-transcribe.mjs, openai-whisper1.mjs, lexicon.mjs, keywords.mjs); contract `transcribeSegment({buffer, mimeType, seq, keywords, promptTail, languageHint}) -> {text, words?, confidence?, flaggedTerms, providerId, modelId, latencyMs}`; no vendor names user-facing; NO MOCKS EVER in StoryForge (hq's mock fallback is quarantined context; StoryForge fails truthfully). Primary `gpt-transcribe` (keywords + prompt + languages; webm/m4a/wav; 25 MB; $0.0045/min snapshot), fallback `whisper-1` (prompt tail only); StoryForge-scoped `STORYFORGE_OPENAI_API_KEY`; Realtime `gpt-live-transcribe` deferred (built only if latency gates fail); Anthropic: no role; new vendors: FG-3.
Runtime: `segmentPlanMs [4000, 15000]`; prompt tail = last 200 chars of prior final; keywords = versioned medical lexicon + draft-title tokens; timeout 30 s; one immediate retry on 5xx/timeout; E6 retries cap 3 with 2 s/8 s backoff; <= 2 concurrent provider calls per session; transcribed segments never resubmitted; session-level failover to fallback on hard errors with `provider_failover` audit; outage = record-now-transcribe-later truthful state; taxonomy `transcribe_unavailable | transcribe_timeout | transcribe_rejected_format | transcribe_failed_permanent`; confidence used for chips only if RP-7/RP-11 prove availability, else lexicon-only, else no chips (never invented uncertainty).
Bake-off (RP-11, activation gate, implementation run): corpus 40 scripted medical passages (medications, procedures incl. Whipple/paracentesis/anastomosis, abbreviations CBC/NSTEMI/PEA/OR/ICU, labs/numbers), >= 6 accent groups by HUMAN readers (founder-side scheduling dependency; TTS only fills vocabulary rows and is excluded from accent scoring), quiet/laptop/phone/noise/whisper conditions, hand-verified hashed references; 3 runs per candidate via the real adapter on the production account; deterministic committed scorer. Gates: WER <= 12% and <= whisper baseline; medical recall >= 92% and >= baseline+10; substitution <= 3%; accent degradation <= 8 pts; first-text p95 <= 10 s; final p95 <= 8 s; failure < 1% after one retry; cost <= $0.01/min. Single outcome table by miss class: accuracy-gate miss -> one keywords retest -> still missing -> whisper-1 primary ONLY if whisper-1 passes accuracy, else activation blocked + FG-3; latency-gate miss -> activation blocked and the Realtime driver lane (gpt-live-transcribe) is PRE-AUTHORIZED for exactly that outcome, re-run gates, still missing -> founder decision (whisper never cures latency); reliability/cost miss -> blocked, evidence to Fable. RP-12: provider training-use/retention/ZDR-BAA posture captured from official pages; conflict with retention policy stops the release lane.

## 7. API and runtime contracts (binding; full table in B1-504A Contracts + carried amendments)

E1 `POST /api/recordings` (scope check; one active session; returns `{recordingId, segmentPlanMs:[4000,15000], caps}`; idempotent per student; 403 `voice_disabled`). E2 `POST /api/recordings/:id/segments` (multipart; idempotent on (session,seq); 413 oversize; writes R2 + enqueues transcription). E3 `GET /api/recordings/:id` (poll 2 s: state, per-segment transcribeState/transcript/flaggedTerms, fullText, totalDurationMs, assembled). E4 `finish` (idempotent; async assembly). E5 `cancel` (idempotent; deletes objects; audits). E6 `retry-transcription` (<= 3/segment). E7 `POST /api/stories` + `recordingId`: EXPLICIT ownership check (caller owns session; state finishing/assembled; not attached; 403 + audit) then transactional attach (story + sf_story_originals reviewed transcript + session attached + asset link, or nothing; draft consumed atomically). E8 `DELETE /api/audio/:assetId` (ownership + policy; OUTSIDE the voice flag). E9 playback (existing; OUTSIDE the flag). E10 `/api/session` + `capabilities.voiceCapture`. E11 admin flags API (admin only; cohort values validated against B1-505; audited; non-admin 403 audited). E12 DECIDED OUT (E3 polling is the heartbeat). E13 `GET /api/admin/voice/health` (admin; metadata only; privileged service query path). Legacy `POST /api/audio/presign` + `POST /api/audio/:id/confirm`: subordinated to the flag + env kill, deployed BEFORE or WITH production R2 config (re-arm hazard). Identity is always derived from verified claims and server-side relationships; client-supplied student IDs are never ownership proof; no internal table shapes on the wire.

## 8. Feature flags and rollout (binding)

`sf_feature_flags` row `voice_capture`; scopes off/allowlist/cohort/eligible_all (founder stage = allowlist with the pilot UUID; valid cohort values from env `STORYFORGE_VALID_COHORTS` set at deploy from the B1-505 quote; empty cohort never grants); PRECEDENCE: env `STORYFORGE_VOICE_FORCE_OFF=1` > DB scope; no force-on; default off; capability computed server-side per request (30 s in-process cache allowed, invalidated on E11 writes; env kill never cached); grace <= 10 min for open sessions on scope change (E1 refuses immediately); env kill refuses everything and the client preserves all text in the draft. Gates recording, upload (segments + legacy presign/confirm), transcription; playback/deletion exempt. Phase 2/3: impossible through this flag; no student-facing AI key exists; `STORYFORGE_AI_*` env flags (V5 scope) remain off and untouched. Rollout: off -> allowlist(founder pilot UUID) at S12/S13 -> allowlist(+test students) S14 -> cohort (exact B1-505 values; G7 first; FG-2 covers S18 narrow pilot AND S20 full 360) -> observation -> eligible_all only under a separate founder ruling. TWO-ACCOUNT RULE: the founder pilot account keeps its student override and performs A2..A22; a separate founder-controlled account receives the admin override (R-8, backup-first, S12) and operates E11/E13; app_role is single-valued and admins have no content access.

## 9. Platform contract (binding; full text in the Platform doc)

Ownership: StoryForge owns stories, originals, versions, lessons, themes, uses, question mappings, preferred mappings, follow-ups, reviews, scores, audio, audits; File Vault owns files; attachment references owned by StoryForge, bytes/permissions by File Vault; consumers NEVER read tables, scrape UI, keep authoritative copies, or re-implement authorization. Stable IDs: existing UUID PKs enumerated (sf_users/sf_stories/sf_story_revisions/sf_audio_assets/sf_questions/sf_story_questions/sf_question_preferences/sf_pair_followups/sf_feedback/sf_audit_events + recording sessions); deleted = 410 tombstone response; archived flagged in summaries; no identity by title/URL/name/position.
Read models (schemaVersion 1; inclusions AND exclusions binding): StorySummary (no text, no comments, no audio access), StoryInterviewContext (working text + lesson + pair data + prepared follow-ups; no mentor drafts; no audio), StoryApplicationContext (adds original telling + reflections; mentor material only with explicit grant and submitted-only), StoryMentorContext (assigned + submitted only), StoryAttachmentContext (references only), StoryTimelineReference (5 fields), StoryChangeEnvelope (content-free).
Service auth: `STORYFORGE_PLATFORM_OFF=1` FORCED in production for all of Phase 1 and no consumer entries exist in production (contract-test lives in CI/controlled env only; S22 verifies both); env-registered consumers (`STORYFORGE_PLATFORM_CONSUMERS`: consumerId, sharedSecret, secretPrev for dual-secret rotation, allowedPurposes, allowedModels, allowedSubjects), HS256 service tokens aud `storyforge-platform`, claims iss/sub(end-user UUID)/act_role/purpose/jti/exp <= 120 s, jti replay cache sized >= rate-limit x TTL; END-USER-PROOF PRECONDITION: before ANY real consumer registers, contract v1.1 must derive sub from the end-user's own StoryForge-verified token, not the consumer's assertion (a shared-secret-only model is explicitly not accepted); a binding purpose-to-model matrix (default deny) governs every call, includeMentor material flows only under mentor_coaching (assignment-checked, submitted-only) or to the story owner on their own submitted story; the changes feed is TENANCY-SCOPED (self, or assigned+submitted for mentors), never org-wide; the audio grant's mentor branch is submitted-only, mirroring E9; purposes `interview_practice | application_writing | mentor_coaching | timeline_reference | attachment_link | audio_playback | change_sync`; per-request assignment checks for mentors; audit `platform_read` (content-free); kill `STORYFORGE_PLATFORM_OFF=1`; domain/admin-role/network position grant nothing.
API `/platform/v1` (60 req/min/consumer; cursor pagination; ETag): implemented dormant in Phase 1 with contract tests: list stories (filters), story summary, current version, interview context, preferred-story resolve, changes feed, use-checks; defined-additive for later: application context, mentor context, attachments CRUD+list, audio playback grant (short-TTL, owner or authorized mentor only), extended search, follow-ups, whoami. Errors: 401/403 coded, 404 never distinguishing private-vs-absent, 410 deleted, 429.
Consent matrix: authenticated self-use of one's own stories (including private) allowed for interview_practice/application_writing/timeline_reference; mentor purposes mirror StoryForge boundaries exactly; LOR mentor-visible material submitted-only, no bulk export; audio only via the grant endpoint; withdrawn designation/archived/deleted reflected immediately + consumer purge; disposable projections, 24 h max TTL, purge on revocation/410, never re-shared, never canonical.
Change propagation: cursor change feed over `sf_audit_events` (append-only, in-transaction; VST), ordered by (created_at, id) with an opaque composite cursor (audit PKs are UUIDs, not insertion-ordered; RP-13 confirms the shape), mapped to the 17 event types (story.access.revoked = mentor reassignment only; consumer revocation is not a feed event, purge triggers on 401 + TTL); pull, at-least-once, replay by cursor; no webhooks/queues/outbox in Phase 1 (anti-invention; upgrade path = outbox behind the same envelope, decided by Fable). Hydration: initial full fetch, incremental cursor, stale markers past TTL, serve-stale-with-marker on outage, purge on revocation.
Consumer examples (binding shapes): IV Prep (preferred -> interview context -> render; audio only self-playback grant), Arena (season projection, refresh on preferred-mapping change), File Vault (attachment reference, no bytes cross, unlink events), LOR Studio (theme-filtered application contexts, submitted-only mentor material, no export), Timeline (reference model, 410 handling). Compatibility: path versioning, additive-never-bumps, /v2 with 6-month overlap for breaking, Sunset headers, contract tests CI-gated from Phase 1, new consumers require Fable review + founder ruling where private-story exposure changes.

## 10. Frontend/backend map (binding; full table in the Map doc)

Voice code lives in `public/app.js` (house single-file pattern; no bundler changes) + `server/recordings.mjs`, `server/transcription/*`, `server/flags.mjs`, mounted from `app.mjs`. Every prototype behavior mapped with PA anchors (openCapture/renderDock/startRec/pauseRec/stopRec/insertFinal/medFlags/discardRec/voxRecover) to named production functions, endpoints, tests, and forbidden deltas; overlap-merge algorithm ported EXACTLY; recovery never auto-starts the mic; typing save path untouched; legacy `toggleRecording` remains only as the flag-off-subordinated legacy path. Browser matrix: Chrome desktop/Android (webm/opus), Safari desktop/iOS (mp4/aac; webm 18.4+), auto-pause on visibility/mute/lock, wake lock with re-acquire, Android ~1 min background loss; second-device transcript recovery supported, second-device mic resume NOT promised; closed-laptop recovery to last uploaded segment + draft. Accessibility: aria-labels, timer aria-live polite, chips as buttons, axe no-regression. Fixed truthful error copy set AUTHORED VERBATIM in the Frontend Map (mic denied, reconnecting, cannot-transcribe-now, unable-to-continue, voice-unavailable-mid-take, daily limit voice_daily_limit 429, length-cap stop); admin Release Controls panel specified as bounded delegation (exact controls incl. allowlist/cohort textareas and disabled eligible_all) and EXEMPT from the pixel matrix; no ad-hoc messages. Test inventory named (unit overlap-merge/segment-plan/lexicon/flags; postgres recording-rls incl. FORCE RLS + cross-student; e2e hero/dock/transcript-check/discard/rollback; conformance flag-off with one enumerated delta).

## 11. Deployment, rollback, observability (binding; full detail in runbooks)

24-step sequence S1..S24 (gate letters G0..G10 remain the B1-504A scheme; FG-2 covers S18 AND S20; retention-copy mechanics pre-authorized for both FG-1 timing cases, incl. an S10b second release path if FG-1 rules late) with commands: baseline hash lock -> existing suites green (`npm test`, `test:postgres`, `test:e2e`, `run-conformance.sh`) -> B1-505 fresh read (quote cohorts) -> backups (Kinsta point, PG dump + restore rehearsal, variable export, WP plugin + option export) -> secrets/R2/key provisioning per specs + `scan:secrets` -> bake-off -> M1/M2 via guarded runner + validation queries -> dormant Railway deploy (healthz + voice_disabled probes incl. legacy endpoints) -> backend suites + platform contract tests -> hidden frontend (build:release + Kinsta install script; flag-off conformance with the single delta) -> screenshot conformance matrix -> founder activation (after R-8 settings change) -> founder full matrix (A2..A22) -> allowlist students -> denied-identity probes (A1, A17b/c) -> browser matrix -> failure drills with per-drill environment and method pinned (provider outage and R2 outage run in the CONTROLLED environment via adapter env override and staging-token revocation; device drills run at founder scope in production) -> pilot cohort (ONE B1-505 value; G7 first) -> 72 h window -> full 360 activation (FG-2) -> 7-day window -> platform CI green -> deployment receipt -> rollback readiness preserved. Zero exit codes are never success; every step files evidence.
Rollback ladder: (1) flag off / env kill (default response; data intact; playback + deletion live) -> (2) transcription-off mode (`STORYFORGE_TRANSCRIBE_PROVIDER=none`, record-now-transcribe-later) -> (3) = (1) typing-only -> (4) Kinsta pointer rollback -> (5) prior Railway build -> (6) migration reversal only with founder authorization + fresh backup -> (7) restore per B1-503 discipline. Trigger table covers provider outage/latency/quality, mic, R2 upload/playback/credentials, regressions, auth/WP claims, RLS (immediate kill + P0 + Fable), CDN cache leak (purge + Fable; no zone edits), migration/DB performance, cost, complaints, platform/change-feed defects (`STORYFORGE_PLATFORM_OFF`), access broadening (immediate narrowest revert + Fable). Invariants: saved stories/transcripts/audio/approved states preserved; deletion rights preserved; no orphaned audio; rollback introduces no exposure.
Observability: event set (recording_started ... platform_denied, sweep_cleaned) with UUID-only correlation and errorCategory `mic|upload|transcribe|assembly|save|auth|platform`; forbidden log contents (story text, transcripts, audio, names, tokens, presigned URLs, secrets, mentor notes) enforced by an acceptance log sweep; REVIEW thresholds evaluated by scheduled operator queries, daily in window 1 and weekly in window 2, reported same-day to the founder (no automated alert evaluator exists or ships in Phase 1) (transcribe failures > 5%/h, upload > 5%/h, playback > 2%/h, provider p95 > 20 s/30 min, cost > 3x, denial spikes); founder triage via E13 by errorCategory; deterministic troubleshooting SQL; incident evidence package; backlog register (CSP pass, transparency surface, DB consumer registry, push delivery).

## 12. Unknowns and probes (the reason for NOT READY)

RP-1 B1-505 (ABSENT at authoring; access lanes blocked; absence not final) · RP-2 worktree git health on the Mac (sandbox mount could not traverse; expected artifact; prove it) · RP-3 live baseline vs receipts, authenticated via the founder pilot session only (release `v-0912286e7dfc2327`, cutover `6f45dbbd...`, Railway `fa7ad084-...`) · RP-4 Cloudflare route audit (worker must be un-routed; active = BLOCKER) · RP-5 Railway services + ALL variable names (SUPABASE* flagged) · RP-6 R2 buckets + config state (public access on any audio bucket = BLOCKER) · RP-7 StoryForge OpenAI key + models list (bounded single-secret rule; hq key models-listing only; gpt-transcribe-absent = flag) · RP-8 ffmpeg feasibility on a LOCAL Nixpacks build (feeds the assembly table) · RP-9 staging existence + local harness boot proof (harness cannot exercise mobile getUserMedia; mobile rows run founder-scope in production by design) · RP-10 WP settings SUMMARY + deployed-plugin hash vs worktree + iframe/microphone-allow check · RP-11 bake-off (implementation run; human corpus scheduling starts now, founder-side) · RP-12 provider data posture. The discovery packet (`B1-504B_CODEX_DISCOVERY_ONLY_PACKET.md`, runs as B1-505D) authorizes ONLY probes RP-1 through RP-13 plus evidence filing; both verbatim no-improvisation rules included.

## 13. Founder gates

FG-1 retention ruling + r2 copy (recommended: approve as specified; default safe: build proceeds, cohort activation blocked; includes the single-copy-audio trade-off acknowledgment, the self-use reuse ruling for platform purposes marked FG-1-conditional in the contract, and the deactivated-student rider: 30-day access wind-down preserving playback/deletion, then support-request deletion). FG-2 cohort activation (G8/G20; blocked on B1-505 + FG-1). FG-3 new transcription vendor (only if bake-off escalates). Standing: no student-facing AI in Phase 1; Phase 2 separate authorization; Phase 3 locked.

## 14. Locations

Repository: `_AI_HANDOFFS/from_cowork/B1-504B_storyforge_v55_infrastructure_platform_authority/` (this package + MANIFEST.sha256). Founder copies: `CLAUDE_FILES/B1-504B_STORYFORGE_V5.5_INFRASTRUCTURE_PLATFORM_AUTHORITY/`. B1-504A package (product authority) and prototypes: locations and hashes per its manifest, re-listed in this package's MANIFEST.sha256. First read: `B1-504B_EXECUTIVE_DECISION_AND_READINESS.md`. Next actions in order: FG-1 ruling; Codex discovery run; B1-505 completion; Fable evidence pass emitting the execution MegaRun; Codex implementation as B1-506.

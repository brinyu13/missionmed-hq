# B1-504B · Codex Discovery-Only Packet (run as B1-505D)

PURPOSE: collect exactly the evidence below and return it to Fable. This packet authorizes NO implementation, NO repository modification beyond writing evidence files under `_AI_HANDOFFS/from_codex/B1-505D_discovery/`, NO migrations, NO deployments, NO configuration changes, NO secret creation, NO destructive git operations. Read-only inspection plus the explicitly listed safe probes only. Two rules, verbatim:
"When repository or production evidence contradicts this authority, do not redesign around the contradiction. Stop the affected implementation lane, preserve the evidence, and return the discrepancy to Fable for an authority amendment."
"Codex may prove a fact. Codex may not convert an unresolved fact into an architectural choice."

For every probe: file the raw evidence (values redacted where marked), state the outcome code, and take ONLY the mandatory action in the table. All lanes are evidence lanes; none may continue into implementation under this packet.

RP-1 B1-505 authority. Probe: check for `_AI_HANDOFFS/from_codex/B1-505_360_BETA_ACCESS_COMBINED_HANDOFF.md` and `B1-505_evidence/B1-505_PRODUCTION_ACCESS_RECEIPT.md`; if present, read fully and quote: access policy, exact cohort values, allowlist state, TTL, run status. Outcomes: COMPLETE+PASS -> quote and file; COMPLETE+FAIL -> file, flag BLOCKER for activation lanes; ABSENT/PARTIAL -> file the listing proof; activation remains blocked. Never reconstruct eligibility from any other source.
RP-2 Worktree git health (the sandbox mount could not traverse the worktree link; expected to be a mount artifact). Probe on the Mac: `git -C /Users/brianb/MissionMed_worktrees/B1-StoryForge-502 status --porcelain && git log -1 --format=%H`. Outcomes: healthy -> record branch/commit and dirty-file list (expected dirty: the B1-504A/B authority folders, which is acceptable and must be committed by the future implementation run, not cleaned); broken link -> BLOCKER, file `git worktree list` output from `/Users/brianb/MissionMed`, no repair attempts.
RP-3 Live baseline identity. Probe: fetch the live `/storyforge/` document and runtime asset hashes AUTHENTICATED AS THE FOUNDER'S PILOT ACCOUNT (the route is access-checked; the founder supplies the session; no other credentials may be used); read the Kinsta pointer (release path) and current Railway deployment ID via dashboards/CLI read-only. Compare to B1-503 receipt values (release `v-0912286e7dfc2327`, cutover `6f45dbbd...`, Railway `fa7ad084-...`) and any B1-505 updates. Outcomes: MATCH -> file; MISMATCH -> BLOCKER to Fable (someone deployed since; authority must re-baseline).
RP-4 Cloudflare routes. Probe: read-only list of Workers routes and DNS for `missionmedinstitute.com`, filtered to `/storyforge*`. Outcomes: no worker route -> file (expected); worker route ACTIVE -> BLOCKER to Fable (topology conflicts with the lock); any other proxy/cache rule on the path -> file verbatim.
RP-5 Railway inventory. Probe: read-only list of services in the project (names, IDs), and for the API service ALL VARIABLE NAMES (names only, no values). Outcomes: file the inventory; absence of expected `STORYFORGE_*` vars is data, not a defect; any `SUPABASE*` name is flagged for Fable (it would contradict the Supabase-null resolution); any unexpected credential-looking name is flagged.
RP-6 R2 state. Probe: read-only bucket list on the Cloudflare account; presence/absence of `STORYFORGE_R2_*` names in RP-5. Outcomes: no StoryForge bucket -> file (creation happens only under future implementation authority); bucket(s) exist -> file names, public-access setting, CORS, lifecycle rules verbatim; any PUBLIC access on an audio bucket -> BLOCKER flag.
RP-7 OpenAI access. Probe: confirm whether `STORYFORGE_OPENAI_API_KEY` exists (name-only from RP-5). For the models probe, Codex is AUTHORIZED to use exactly one secret value, in a non-logging shell, never written to any file: the StoryForge key if present, else the existing hq `OPENAI_API_KEY` (listing models only; the hq key may NEVER be used for transcription calls under any packet). Call `GET /v1/models`; record whether `gpt-transcribe` and `whisper-1` are listed. Outcomes: both listed -> file; StoryForge key absent -> file (provisioning is implementation work); `gpt-transcribe` NOT listed -> flag for Fable (contradicts the provider lock; the amendment pass resolves before the MegaRun); no key of either name exists -> file, provider probes deferred to implementation.
RP-8 ffmpeg feasibility. Probe: in a LOCAL container built from the repo's Nixpacks configuration (never the production service), run `ffmpeg -version`; if absent, add ffmpeg to a LOCAL COPY of the build config kept under the evidence folder (the repo's own config files are not modified) and rebuild locally; then concat-remux the 40-segment 10-minute fixture set (generate fixtures locally with tone audio or free local TTS; no paid API calls) and record wall time and playback integrity in Chrome and Safari. All RP-8 artifacts live under the evidence folder. Outcomes: feed the binding assembly decision table in the Storage Spec (Option A vs Option B); file timings.
RP-9 Staging. Probe: search Railway project and repo scripts for any StoryForge staging service/database; run `bash scripts/run-local.sh` locally to prove the harness boots. Outcomes: staging exists -> file identifiers; absent -> file; harness broken -> file exact failure (BLOCKER for the controlled-test lane).
RP-10 WordPress production settings + plugin identity + embedding. Probe: read-only `wp option get missionmed_storyforge_settings` (or DB read) capturing AS SUMMARY, not verbatim dump: storyforge_enabled; allowed_user_ids COUNT and founder-present yes/no; app_role_overrides COUNT and a role histogram (do not list the full user-to-role map in repo evidence; it is an access-control snapshot); allowed_roles; allowed_cohorts values; token_ttl_seconds. PLUS: hash the deployed `missionmed-storyforge-sso.php` plugin file and compare to the worktree copy (the cohort-claim code must exist in the DEPLOYED plugin; a mismatch reopens the R-6 plugin-change lane and is a flag to Fable). PLUS: record whether the live app renders in an iframe. Outcomes: file; iframe present -> the embed must carry `allow="microphone"` in implementation; plugin hash mismatch -> flag.
RP-11 Provider bake-off. NOT part of this packet (it requires the adapter build and corpus recordings). Explicitly deferred to the implementation run; corpus HUMAN RECORDING SCHEDULING is a founder-side dependency to begin now (list of accent groups in the B1-504A bake-off doc).
RP-12 Provider data handling. Probe: capture current official OpenAI API data-usage/retention/ZDR/BAA statements (page snapshots + URLs). Outcomes: file; material conflict with the approved retention policy -> BLOCKER flag for Fable.
RP-13 Database introspection (read-only psql via the guarded connection). Capture: (a) role attributes: `SELECT rolname, rolbypassrls, rolsuper, rolcanlogin FROM pg_roles WHERE rolname IN ('anon','authenticated','storyforge_app');` plus which role the SERVICE connection actually authenticates as (from the connection string name only, value redacted); (b) `sf_users` column list; (c) `sf_audit_events` column list and PK type (UUID vs serial) and whether a serial/sequence column exists; (d) the story-deletion model: does any code path hard-DELETE `sf_stories` (grep handlers) and what `sf_stories.status` values exist in CHECK constraints (archived? deleted?); (e) guarded-runner transaction behavior: does `apply-production-migrations.sh` wrap files in its own transaction (read the script; if yes, the M1/M2 embedded BEGIN/COMMIT are removed by Fable in the amendment pass, a mechanical edit); (f) grants and owner of two existing tables (`sf_stories`, `sf_audio_assets`) as the pattern reference. Outcomes: file all; these feed the DB spec's decision tables (story deletion, seq ordering, service-role posture) in the Fable amendment pass.

RETURN FORMAT: one file `B1-505D_DISCOVERY_EVIDENCE.md` with a section per probe (outcome code first line), plus raw captures in the evidence folder, plus a one-page summary table. Fable consumes this, closes the readiness conditions, amends where evidence contradicts, and only then issues `B1-504B_CODEX_5.6_SOL_ULTRA_EXECUTION_MEGARUN.md`.

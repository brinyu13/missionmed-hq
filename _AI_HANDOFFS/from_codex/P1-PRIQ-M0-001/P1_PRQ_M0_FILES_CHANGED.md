# Files changed

New implementation roots: `apps/priq-api`, `apps/priq-web`, `packages/mir-core`, `packages/mir-providers`, `packages/mir-queue`, `packages/mir-telemetry`, `config/priq`, `tests/priq`, and `infra/priq/migrations`.

Root changes: `.gitignore`, `.env.example`, `package.json`, `package-lock.json`, and `tsconfig.json`. No unrelated product application or production configuration was modified.

# HERSCHEL — Production Discovery and Mapping

Recorded: 2026-07-27T16:40:59Z

Scope: B1-502M Wave 1 read-only discovery.

Constraints honored:

- No production endpoint, provider API, SSH host, DNS resolver, database, or remote Git service was contacted.
- No production, remote, authority, source, configuration, or credential state was mutated.
- No secret values or private user/student data were read or recorded.
- The only file written by HERSCHEL is this report.

## Evidence labels

- **VERIFIED LOCAL** — directly observed in current local files, Git objects, or local repository metadata.
- **RECORDED PRODUCTION EVIDENCE** — a local receipt claims a prior production observation or mutation; it was not reverified live in this wave.
- **STALE/CONFLICTING** — older or noncanonical evidence contradicts a newer local record or cannot meet current deploy-baseline rules.
- **UNRESOLVED** — current local evidence does not establish the fact.
- **CANDIDATE** — a proposed production component or mutation, not current runtime truth.

## 1. Executive architecture verdict

The B1-501 candidate supplies three coherent local seams:

1. an isolated WordPress SSO/entitlement plugin;
2. a Cloudflare Worker configuration for same-origin `/storyforge/*`;
3. a mounted StoryForge V5 SPA and Node/PostgreSQL application.

Those seams are locally verified but are not yet bound to an established production runtime owner:

- the WordPress plugin has no proven Kinsta installation/activation workflow or target receipt;
- the proposed Matrix navigation filters do not exist in the current protected Matrix source candidates;
- the Worker names a route and zone but no account, existing project, deployment record, or protected origin binding;
- the Node backend has no standalone production hosting descriptor or registered runtime owner;
- the PostgreSQL migration explicitly says its target project is unpinned;
- current protected Matrix recovery sources exist only as untracked local export/candidate artifacts.

The best local operational record of the current Matrix production shape remains the active Matrix lock at `/Users/brianb/MissionMed/_SYSTEM/KNOWN_GOOD/MATRIX_RUNTIME_LOCK_MANIFEST.json`. It records the legacy Matrix-owned route `https://missionmedinstitute.com/member-dashboard/#storyforge`, not V5 `/storyforge/`. That lock itself is modified but uncommitted in the dirty `/Users/brianb/MissionMed` checkout, so it is operational evidence, not a clean committed deployment baseline.

## 2. Repository and control-plane map

### StoryForge implementation repository

**VERIFIED LOCAL**

| Fact | Value |
|---|---|
| Worktree | `/Users/brianb/MissionMed_worktrees/B1-StoryForge-502` |
| Git common directory | `/Users/brianb/MissionMed/.git` |
| Branch | `b1-502-storyforge-production-deployment` |
| Current HEAD | `e76193176e50fa0f0c329b40017c3e48b94510ef` |
| B1-501 implementation baseline | `5ba56c7e3dd4f251ef4fc66c9de5fc4300c8acbc` |
| B1-500 foundation | `be43c6d0a4520ed761a3d112a25452f26683f9ca` |
| Locally configured remote | `https://github.com/brinyu13/missionmed-hq.git` |

The commit chain is:

```text
be43c6d B1-500 local foundation
  -> 5ba56c7 B1-501 Matrix seams
    -> e761931 B1-502 blocked-gate evidence
```

No local remote-tracking branch contains `5ba56c7` or `e761931`; actual GitHub presence is **UNRESOLVED** because no remote was contacted.

### MissionMed OS

**VERIFIED LOCAL**

| File | Observed state |
|---|---|
| `/Users/brianb/MissionMed_OS/BOOT.md` | Active boot contract |
| `/Users/brianb/MissionMed_OS/CURRENT.md` | Generated 2026-07-22T08:55:20-04:00; no B1-502M |
| `/Users/brianb/MissionMed_OS/missions.json` | No B1-500/B1-501/B1-502/B1-502M mission |
| `/Users/brianb/MissionMed_OS/products_index.json` | StoryForge is `protected_active_or_recent`; `passport_path` is `null` |
| `/Users/brianb/MissionMed_OS/authority_index.json` | Routes StoryForge to the Matrix lock plus Critical Systems, Data Flow, Supabase, and execution contracts |

`CURRENT.md` is generated by `/Users/brianb/MissionMed_OS/tools/mmos_status.py`; the repository README explicitly forbids hand editing it. The OS checkout is on `main` at `f197c54a9d5b062fa3c8e773bc19c64de9dba6cb` and has unrelated untracked directories. No control-plane write was attempted.

### Governing production records

**VERIFIED LOCAL**

- `/Users/brianb/MissionMed/_SYSTEM/CODEX_EXECUTION_GUARDRAILS.md`
- `/Users/brianb/MissionMed/_SYSTEM/CRITICAL_SYSTEMS_CONTRACT.md`
- `/Users/brianb/MissionMed/_SYSTEM/CRITICAL_SYSTEMS_MANIFEST.json`
- `/Users/brianb/MissionMed/_SYSTEM/KNOWN_GOOD/MATRIX_RUNTIME_LOCK_PROTOCOL.md`
- `/Users/brianb/MissionMed/_SYSTEM/KNOWN_GOOD/MATRIX_RUNTIME_LOCK_MANIFEST.json`
- `/Users/brianb/MissionMed/_SYSTEM/tools/matrix_runtime_guard.py`
- `/Users/brianb/MissionMed/_SYSTEM/SUPABASE_MIGRATION_PROTOCOL.md`

The Critical Systems manifest requires every new route, asset, RPC, iframe, CDN key, or Matrix app mode to have an owner, runtime owner, source of truth, route/asset checks, dependency pins, browser expectation, and rollback. It presently has no V5 `/storyforge/*`, StoryForge V5 origin, StoryForge database, or `missionmed-storyforge-sso` registration.

## 3. Current Matrix and legacy StoryForge

### Best locally recorded current shape

**VERIFIED LOCAL as a local lock; not live-reverified**

The root Matrix lock records:

- lock update time: `2026-07-15T11:43:51.670Z`;
- production SSH alias: `missionmed-kinsta`;
- Kinsta plugin root: `/www/theresidencyacademy_209/public/wp-content/plugins/missionmed-hub`;
- public asset base: `https://missionmedinstitute.com/wp-content/plugins/missionmed-hub`;
- broad cache purge prohibited;
- fresh Kinsta backup required before deploy;
- protected legacy StoryForge route: `https://missionmedinstitute.com/member-dashboard/#storyforge`.

Locked StoryForge legacy assets:

| Asset | Recorded production path | Recorded approved SHA-256 | State |
|---|---|---|---|
| `storyforge_js` | `assets/student-os-storyforge.js` | `a4aa9665012206771fc8549c897cb5d22801899347c706626062dbafb29c81fa` | Matrix Runtime v2 static, no-persistence 360 bootstrap |
| `storyforge_css` | `assets/student-os-storyforge.css` | `5b0426a7af9dbc36a1401c5d2829ca8cf7827e8070b783fbfe64875c847af7d8` | Legacy Matrix app-mode CSS |

The same lock records the current shell and WordPress integration candidates:

- fingerprinted Matrix shell JS SHA-256 `c1d97237eab4936d014ec00549deb2358a056d5b8f430fe7713f5dd2ac39e76a`;
- `class-mmed-student-os.php` SHA-256 `5ed6e92eb9bf748a01f475bc5a6a72e249e21a2b7560d07d2acf66f8058e8d95`.

### Recorded deployment provenance

**RECORDED PRODUCTION EVIDENCE**

Local receipts attribute the legacy StoryForge deployment to:

- `B1-Storyforge-100-d-a` on 2026-06-23: initial four-file Matrix deployment with backups under `/www/theresidencyacademy_209/private/matrix-runtime-guard-backups/B1-Storyforge-100-d-a/`;
- `B1-Storyforge-100-g-a` on 2026-06-30: guarded deploy of `student_os_js`, `class_mmed_student_os_php`, and `storyforge_js`, with backup root `/www/theresidencyacademy_209/private/matrix-runtime-guard-backups/B1-Storyforge-100-g-a/20260630T094715Z`;
- `Y1-CAM-4005R` and `Y1-CAM-4006` on 2026-07-15: later Matrix shell/PHP revisions recorded in the current local lock.

The legacy StoryForge JS/CSS bytes matching the recorded hashes exist at:

- `/Users/brianb/MissionMed_worktrees/b1-storyforge-advanced-102-live-matrix-source-export/wp-content/plugins/missionmed-hub/assets/student-os-storyforge.js`
- `/Users/brianb/MissionMed_worktrees/b1-storyforge-advanced-102-live-matrix-source-export/wp-content/plugins/missionmed-hub/assets/student-os-storyforge.css`

The current shell/PHP candidate bytes matching the lock exist at:

- `/Users/brianb/MissionMed_worktrees/Y1-CAM-3000/Y1-CAM-4005R/candidates/matrix/student-os.js`
- `/Users/brianb/MissionMed_worktrees/Y1-CAM-3000/Y1-CAM-4006/candidates/matrix/class-mmed-student-os.php`

### Source integrity limitation

**UNRESOLVED / deploy-blocking under current contracts**

- The protected `missionmed-hub` source is absent from the B1-502 worktree.
- The source-export worktree is at old commit `38ad5fb...` with the entire `wp-content/plugins/` package untracked.
- The Y1-CAM source/evidence directories are also untracked in their worktree.
- `/Users/brianb/MissionMed` is broadly dirty, and its current Matrix lock changes are uncommitted.

Therefore matching bytes are available for recovery/comparison, but there is no clean committed canonical protected source baseline established by local evidence.

### Legacy V3 is not current authority

**STALE/CONFLICTING**

The source-export worktree also contains `student-os-storyforge-v3.js/.css` and V3 wiring in its untracked PHP/JS. The current lock has no `storyforge_v3_js` or `storyforge_v3_css` entry, and the current Y1-CAM shell/PHP candidates do not contain that V3 wiring. These V3 artifacts are not current deploy authority and must not be promoted as the current production StoryForge runtime.

## 4. WordPress and Matrix deployment mechanisms

### Proven mechanism for protected `missionmed-hub`

**VERIFIED LOCAL mechanism; production use not exercised in this wave**

`matrix_runtime_guard.py guarded-deploy`:

1. loads the Matrix lock;
2. verifies selected local source files;
3. creates a fresh Kinsta backup below `/www/theresidencyacademy_209/private/matrix-runtime-guard-backups/<ticket>/<timestamp>`;
4. uploads only selected assets through `scp` to the manifest’s Kinsta plugin root;
5. verifies remote-origin SHA-256;
6. verifies public cache-busted SHA-256 where applicable;
7. writes a local deployment report.

This mechanism covers registered `missionmed-hub` asset keys. It does not presently register or deploy the separate `missionmed-storyforge-sso` plugin.

### B1-501 isolated WordPress plugin

**CANDIDATE**

Source:

- `wp-content/plugins/missionmed-storyforge-sso/missionmed-storyforge-sso.php`
- `wp-content/plugins/missionmed-storyforge-sso/uninstall.php`
- `wp-content/plugins/missionmed-storyforge-sso/README.md`

The plugin:

- defaults `missionmed_storyforge_settings.storyforge_enabled` to false;
- supplies WordPress AJAX bootstrap and `POST /wp-json/missionmed/v1/storyforge/token`;
- gets its signing key only from protected environment/constant configuration;
- derives admin access from `manage_options`;
- fails closed for unavailable student entitlement, mentor assignments, UUID mappings, signer, nonce, session, origin, or cohort;
- adds candidate Matrix navigation/tile filters and WordPress menu/shortcode fallbacks.

**UNRESOLVED compatibility finding:** the proposed filters `missionmed_matrix_navigation_items` and `missionmed_matrix_dashboard_tiles` do not appear in the current protected Matrix source candidates. The current Matrix entry is constructed by `class-mmed-student-os.php` plus `student-os.js` as a hash route and runtime module. Production V5 navigation therefore requires a verified interface repair or a different proven hook; installing the isolated plugin alone does not prove a Matrix sidebar/tile launch.

No local guarded installer, Kinsta target path, WP-CLI activation receipt, option-setting receipt, or rollback receipt exists for this separate plugin.

## 5. Proposed edge/CDN/DNS/origin topology

### Candidate request flow

```text
Matrix / WordPress session
  -> Matrix V5 launch link
  -> https://missionmedinstitute.com/storyforge/
  -> Cloudflare Worker route missionmedinstitute.com/storyforge/*
       -> static ASSETS for SPA/index/deep links
       -> STORYFORGE_ORIGIN for /storyforge/api/* and /storyforge/healthz
  -> SPA calls same-origin WordPress AJAX bootstrap and REST token endpoint
  -> short-lived JWT
  -> StoryForge Node API
  -> PostgreSQL RLS/RPCs
  -> optional private R2 audio storage
```

### Edge candidate

**VERIFIED LOCAL candidate**

`storyforge-v5/infra/edge/wrangler.toml` declares:

- Worker name `missionmed-storyforge-v5`;
- `workers_dev=false`;
- preview URLs disabled;
- static assets from `storyforge-v5/dist`;
- route `missionmedinstitute.com/storyforge/*`;
- base path `/storyforge/`.

`storyforge-v5/infra/edge/worker.mjs`:

- serves static SPA assets and deep-link fallback;
- proxies only `healthz` and `api/*` to `STORYFORGE_ORIGIN`;
- sets HTML `no-store`, fingerprinted assets immutable, and other responses `no-cache`;
- returns 404 outside the StoryForge prefix.

### Edge/DNS/origin gaps

**UNRESOLVED**

- No Cloudflare account ID, zone ID, existing Worker/project ID, provider revision, or deployment receipt is in the candidate.
- `STORYFORGE_ORIGIN` is intentionally absent from source.
- No local DNS/route precedence export establishes whether another Worker, Pages route, proxy, or WordPress rule currently owns `/storyforge/*`.
- No `.openai/hosting.json` exists in the B1-502 or MissionMed root.
- No production cache-rule export or independent route-removal receipt exists.
- No remote route, DNS, CDN, or origin inspection occurred in this wave.

### Application origin

**CANDIDATE / UNRESOLVED owner**

The backend entry is `storyforge-v5/server/app.mjs`; it requires a database URL, public origin, Matrix URL, allowed origins, JWT verification configuration, and optional private R2 configuration.

There is no StoryForge-specific `Dockerfile`, `railway.json`, `Procfile`, `render.yaml`, `fly.toml`, or Sites hosting descriptor. The repository-level `railway.json` starts `node missionmed-hq/server.mjs`, and the Critical Systems manifest names only `missionmed-hq/` as the active source root for the existing Railway production service. That runtime has no StoryForge route. The verified existing Railway service is not a proven StoryForge V5 origin.

## 6. PostgreSQL/Supabase and assignment map

### Candidate schema

**VERIFIED LOCAL candidate**

The additive migration is:

`storyforge-v5/infra/postgres/migrations/20260726150000_b1_500_storyforge_v5_foundation.sql`

SHA-256:

`93018d16582890890ac9ad696cdfd11b5d8118afa55a709725c531a52fae6a1f`

Its header states:

`Target project: UNPINNED — DO NOT APPLY OUTSIDE THE ISOLATED TEST HARNESS.`

It defines 15 `public.sf_*` tables with RLS enabled, including `sf_users`, `sf_mentor_assignments`, `sf_stories`, revisions, feedback, notifications, audit events, audio assets, questions, workshops, imports, and AI suggestions, plus security-definer workflow functions and append-only/immutability triggers.

### Project clues

**VERIFIED LOCAL**

- The B1-502 worktree has no `supabase/.temp/project-ref`.
- Root `/Users/brianb/MissionMed/supabase/.temp/project-ref` points to the already registered RanklistIQ project.
- The Critical Systems manifest registers RanklistIQ, Growth Engine, and Scheduler Staging, but none assigns StoryForge ownership.
- Root `supabase/migrations` contains no `sf_*` migration.

The root link is only a clue about the general MissionMed checkout; it is not evidence that RanklistIQ owns StoryForge. StoryForge project, migration history, RLS presence, backup/PITR, region, connection owner, and deployment mechanism remain **UNRESOLVED**.

### Assignment ownership

**CANDIDATE / UNRESOLVED production source**

The plugin defaults to:

- WordPress-to-StoryForge ID meta: `_missionmed_storyforge_user_id`;
- mentor assignment meta: `_missionmed_storyforge_student_ids`;
- mentor roles: `mentor`, `advisor`, `coach`;
- database enforcement: `public.sf_mentor_assignments`.

These are locally tested adapters, not a discovered production owner. The MegaRun decision to leave mentor access disabled can isolate founder launch from this unresolved optional path, but no production assignment contract has been proven.

## 7. Exact candidate changed systems and files

### Existing committed B1 candidate

**VERIFIED LOCAL**

Deploy-affecting committed sources are:

- SPA/source: `storyforge-v5/public/index.html`, `app.js`, `auth.js`, `styles.css`;
- built static candidate: `storyforge-v5/dist/index.html` and `dist/assets/*`;
- application origin: `storyforge-v5/server/app.mjs`, `auth.mjs`, `config.mjs`, `db.mjs`, `imports.mjs`, `storage.mjs`;
- database: `storyforge-v5/infra/postgres/migrations/20260726150000_b1_500_storyforge_v5_foundation.sql`;
- edge: `storyforge-v5/infra/edge/worker.mjs`, `wrangler.toml`;
- WordPress: `wp-content/plugins/missionmed-storyforge-sso/*`.

Built artifact hashes:

| Artifact | SHA-256 |
|---|---|
| `dist/index.html` | `ab6ef723444d5fbde8d651d8252423820972bee92670f0741a9db34dda69ba9c` |
| `dist/assets/app.af177a227970.js` | `af177a227970f40d65fd07318526fcf66e3f5d9fe7e947bd2c1f9dcd0c0f1659` |
| `dist/assets/auth.888214ef5fbc.js` | `888214ef5fbcfb81854aa740a1cea7ca892bea4ae21329818efa90e88e6fac7e` |
| `dist/assets/styles.6c4c7db3b9f3.css` | `6c4c7db3b9f3ce781487ee136643fc404f6a04ed40e95e8a191df473263e0ee7` |

### Provider/system mutations that a real deployment would require

**CANDIDATE until exact ownership is proven**

1. Install/activate the isolated WordPress plugin on Kinsta with protected signer configuration and default-off option state.
2. Add a Matrix-owned V5 launch entry using a proven current Matrix hook; likely protected PHP/JS involvement must be scoped only after a clean current source baseline is created.
3. Create/update the Cloudflare Worker, asset deployment, `/storyforge/*` route, cache behavior, and protected `STORYFORGE_ORIGIN`.
4. Create/select and deploy the StoryForge application-origin service.
5. Select the StoryForge PostgreSQL project, create verified backup/PITR, apply the isolated migration through the approved history workflow, and configure server-only connectivity.
6. Optionally configure private R2 audio storage; it is not required for a founder launch if left truthfully unavailable.
7. Register B1-502M, product passport, runtime owners, routes/assets, browser journey, feature flag/cohort, deploy/rollback owners, and evidence in MissionMed OS and critical manifests.

Potential protected Matrix files are not yet an approved change list. The current interface boundary indicates that any repair would most likely involve the clean current equivalents of:

- `wp-content/plugins/missionmed-hub/includes/class-mmed-student-os.php`;
- `wp-content/plugins/missionmed-hub/assets/student-os.js` or its fingerprinted successor.

The legacy `student-os-storyforge.js/.css` should remain unchanged as fallback unless a separately reviewed plan proves a change is necessary.

## 8. Resolvable gaps and external blockers

### Concrete gaps for the Supervisor’s next resolution wave

1. Reconcile and commit a clean canonical current Matrix source baseline; current matching sources are untracked.
2. Refresh MissionMed OS and register B1-502M/passport through normal writers.
3. Inspect the authenticated Kinsta environment for the exact plugin install/activate/rollback path.
4. Inspect the authenticated Cloudflare account/zone for existing route precedence and select/create the exact Worker target.
5. Select and register an application-origin runtime; the existing HQ Railway service is not that owner.
6. Pin the exact StoryForge PostgreSQL/Supabase project and migration history.
7. Prove founder-only navigation and entitlement against the current Matrix interface, since the B1-501 filter hooks are absent.
8. Create target-specific restore points only after those targets are resolved.

### Genuinely external blockers found by HERSCHEL

None proven from local discovery alone.

Provider authentication, account ownership, billing, interactive 2FA, and production availability were not tested. They remain **UNRESOLVED**, not established external blockers.

## 9. Direct evidence index

- MegaRun authority: `/Users/brianb/.codex/attachments/2fbc920d-dd6a-4202-8f76-774822b20522/pasted-text-1.txt`
- B1-501 combined handoff: `_AI_HANDOFFS/from_codex/B1-501_storyforge_v5_integration/B1-501_COMPLETE_COMBINED_HANDOFF.md`
- B1-502 combined handoff: `_AI_HANDOFFS/from_codex/B1-502_storyforge_production_deployment/B1-502_COMPLETE_COMBINED_HANDOFF.md`
- Current local Matrix lock: `/Users/brianb/MissionMed/_SYSTEM/KNOWN_GOOD/MATRIX_RUNTIME_LOCK_MANIFEST.json`
- Matrix guard mechanism: `/Users/brianb/MissionMed/_SYSTEM/tools/matrix_runtime_guard.py`
- Critical manifest: `/Users/brianb/MissionMed/_SYSTEM/CRITICAL_SYSTEMS_MANIFEST.json`
- Legacy deploy receipts:
  - `/Users/brianb/MissionMed/_AI_HANDOFFS/from_codex/B1-MacPro-Storyforge-ADVANCED-Codex-ExtraHigh-100-d-a_Report.md`
  - `/Users/brianb/MissionMed/_AI_HANDOFFS/from_codex/B1-MacPro-Storyforge-ADVANCED-Codex-ExtraHigh-100-g-a_Report.md`
- Current shell/PHP recorded evidence:
  - `/Users/brianb/MissionMed_worktrees/Y1-CAM-3000/Y1-CAM-4005R/evidence/4005r_final_live_hashes.json`
  - `/Users/brianb/MissionMed_worktrees/Y1-CAM-3000/Y1-CAM-4006/evidence/4006_production_release.json`

## 10. HERSCHEL conclusion

The route target and release cohort are founder-decided, but the production topology is not yet resolved by current local evidence. The smallest viable architecture is the B1-501 three-seam design, provided the Supervisor first binds four missing owners: isolated WordPress deployment, Matrix launch interface, Cloudflare route/application origin, and StoryForge PostgreSQL project. The existing legacy Matrix StoryForge route is a protected static no-persistence fallback and should be preserved during V5 founder rollout.

## 11. Focused Cloudflare prestate closure

**Recorded:** `2026-07-27T18:12:11Z`

**Mode:** read-only official provider inspection

**Receipt:** `../evidence/provider-prestate/CLOUDFLARE_ROUTE_CACHE_PRESTATE.md`

### Provider identity

**VERIFIED**

- Cloudflare account ID:
  `eeaaf73d1670b47a162d251ca67e7cfa`
- `missionmedinstitute.com` zone ID:
  `7549e75c42eeef33eafbd071b3142b14`
- The exact account/name-filtered lookup returned one active, full, unpaused
  zone.

### Worker routes

**PASS**

The complete zone Worker-route result contained eight routes. Both requested
StoryForge patterns are absent:

- `missionmedinstitute.com/storyforge`
- `missionmedinstitute.com/storyforge/*`

No existing route pattern matches the apex exact path, trailing-slash path, or
descendant probe under either HTTP or HTTPS. Consequently no current exact,
prefix, catch-all, wildcard-host, or no-script route participates in Worker
precedence for these StoryForge paths.

### Cache configuration

**UNRESOLVED — AUTHORIZATION BLOCKED**

The authenticated Wrangler OAuth credential successfully reads the zone and
Worker routes, but Cloudflare returned HTTP `403` for:

- zone Rulesets list;
- `http_request_cache_settings` entrypoint;
- `http_response_cache_settings` entrypoint;
- active legacy Page Rules list.

Wrangler `4.114.0` has no read/list command for these zone cache products, and
the only trigger subcommand is a mutating deploy operation, which was not run.
The exact remaining authority is a read-only credential with **Cache Rules
Read** and **Page Rules Read** for this zone. Until that is supplied, whether a
zone cache rule matches `/storyforge` remains unknown rather than absent.

No token, email, unrelated route detail, or provider response body was
persisted. All provider calls were GET-only; no remote or production mutation
occurred.

# B1-510I Motion and Branding Design Receipt

Status: **IMPLEMENTED AND DEPLOYED**.

The opening hierarchy is:

1. `Dr Brian's IV Prep On-Call`
2. `MissionMed Institute`
3. `Mission:Residency Division`
4. `StoryForge`
5. `Opening your private story workspace…`

The exact official MissionMed PNG is bundled inside the immutable release, not hot-linked. Its SHA-256 is `f091d62ac5842cde0e9e455321839fd98b291598478aae6ce13b09ea3896ff56`.

The existing aurora and canvas system was extended rather than replaced. Deterministic particles and the four bounded energy states preserve the canonical dark product and do not reconstruct the application shell. `STORYFORGE_PREMIUM_MOTION=0` remains the instant rollback control; disabling it removes presentation motion without affecting StoryForge functions.

Live production reports `premiumMotion=true`; the exact deployed app and stylesheet hashes match the deterministic release. Local visual checkpoints cover Founder admin review, launch branding, active capture, recording energy, and reduced motion.

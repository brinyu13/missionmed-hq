# Sagan evidence report

PASS for the repaired truth chain.

- The answered question, applicant answer, and completed next interviewer
  utterance are stored and exported separately.
- Requested model, provider-returned interviewer model, and provider-returned
  observer model are separate per-turn fields.
- Plan exhaustion fails closed unless the observer confirms a real final or
  terminated utterance.
- Conduct termination speaks and displays the exact final boundary, stops the
  next mic turn, and enters self-rating.
- Media is labeled Replay only when a real `blobUrl` exists; metrics-only
  records are labeled Evidence / Results or NO MEDIA.
- `W. Clint Oxley` remains an unverified founder preference and is not falsely
  bound to `cedar`.

Authenticated smokes returned exact provider IDs `gpt-5.6-terra`,
`gpt-5.6-luna`, `gpt-realtime-2.1`, and voice `cedar` as documented in the
combined handoff.

# Ecosystem Safety

## Isolation

All Gold Set text, student aliases, question/answer relationships, locators, and ambiguity-review material are confined to the approved owner-only `TARGET_BOUNDARY`. The boundary and all working directories are owner-only (`0700`); restricted files are owner-only. Its `raw/` directory is empty because source pairs were streamed read-only from `SOURCE_BOUNDARY`.

Git contains only schemas, deterministic tooling, synthetic fixtures, aggregate counts, position-only ledgers, provenance roots, validation receipts, and reports. Safe rows omit source aliases, drill IDs, question IDs, student aliases, wording, locators, source hashes, and shard paths.

## Storage recovery

I1Q-1008F-R restored the execution floor using verified disposable installers, exact duplicate files, cache contents, and invalid Finder metadata. When Claude later regenerated a 16.1 GiB VM runtime bundle, Claude was gracefully closed and only that verified recreatable bundle was removed; local-agent project/session data remained unchanged. Active browser and GeoServices caches, system/update snapshots, synchronized storage, repositories, worktrees, production artifacts, and restricted data were not cleanup targets. Final readings exceeded 25 GiB available. Details are content-addressed in `evidence/storage-recovery-receipt.json`.

## Repository and platform invariants

- No production, CDN, R2, database, API, registry, auth, bootstrap, Matrix, Arena, STAT, MMVS, or learner-facing mutation was authorized or performed.
- The main repository HEAD, branch, valid refs, and worktree registry remained unchanged through recovery.
- Full Git object verification exited successfully; unreachable dangling objects were retained and no prune or garbage collection was run.
- The 1008E source boundary remained owner-only with 4,148 files.
- Google Drive was not used; restricted data never left local storage.
- No final MCQ generation, physician approval, release, or historical-universe completeness claim is part of this mission.

## Rollback

The safe Git handoff is isolated to this task directory and can be reverted as one commit. Restricted 1008F output is a new derived boundary; removing that boundary does not change 1008E or any production consumer. No live rollback procedure is required because no live state is changed.

Sentinel's postflight passed contract/root, 97-shard, raw-empty, permission, handle, reserve-space, Git-integrity, source-count, and zero-production-mutation gates. One exact absolute governance locator is allowlisted only in the filed protected-path cleanup decision; all reports, ledgers, evidence, and handoffs use boundary aliases.

# Parallel Factory Integration

Existing authorities were located:

- cost router: `P1_RISE_PARALLEL_COST_QUALITY_OPTIMIZATION_007`
- continuous factory: `P1_RISE_PARALLEL_CONTINUOUS_FACTORY_003/service`

The 5007 preview names the validated router policy and supplies a narrow exact-claim task class. It prioritizes a student-supplied URL and retains fields for the selected product/processor and estimated cost.

No secure bounded Railway-to-factory bridge or separately approved Student Intel budget was found. Accordingly:

- the app does not select Core/Pro/Ultra by guess;
- estimated cost remains `null`;
- paid submission is denied;
- the twice-monthly schedule is inactive;
- high-priority items remain queued without bypassing spend controls.

The existing Sonnet application-season campaign was not modified. Migration 006 rejects the protected campaign ID `RISE-BOOTSTRAP-001` for Student Intel verification runs, and no factory/provider mutation was performed.

Status: **interface staged; factory not connected**.


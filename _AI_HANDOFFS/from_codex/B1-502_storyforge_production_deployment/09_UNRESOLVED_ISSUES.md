# B1-502 Unresolved Issues

These are hard blockers before any production contact or mutation:

1. Refresh MissionMed OS `CURRENT.md` through the normal control-plane writer and register/route B1-502 in `missions.json`.
2. Register the V5 `/storyforge/*` route, assets, browser journey, runtime owner, and rollback owner in current Critical Systems/Matrix authority without replacing the protected legacy lock by inference.
3. Pin the exact canonical WordPress/Matrix production environment, current revision, deploy source/branch, and authorized plugin deploy mechanism.
4. Pin the exact edge/CDN account, zone, project/worker, `STORYFORGE_ORIGIN`, route precedence, cache rules, and independent rollback mechanism.
5. Pin the authoritative production mentor-assignment owner and contract; ratify stable identifiers and staff role mapping; run a privacy-safe read-only production reconciliation.
6. Pin the exact StoryForge Supabase project, migration history, server-side sync credentials, backup/PITR capability, and restoration owner.
7. Create and verify fresh readable restore points for every exact system to be mutated.
8. Name the smallest founder/admin test cohort, deployment window, and on-call rollback operator.

Founder approval for the bounded release is already present. It does not resolve these technical and control-plane facts.

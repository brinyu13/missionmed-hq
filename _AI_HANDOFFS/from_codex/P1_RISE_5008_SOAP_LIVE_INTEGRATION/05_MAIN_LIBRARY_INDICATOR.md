# Main Library Indicator

Find Programs rows/cards render a bounded `SOAP` historical-cycle signal only when canonical SOAP history exists. Program File renders track-separated reported positions with a non-predictive context statement. The indicator does not claim current vacancies or future match likelihood.

Accessibility semantics and all core routes passed the browser critical-violation scan.


# SOAP Explorer Implementation

The founder-approved Fable shell receives an additive `SOAP Explorer` rail item and `#/soap` route. The view uses canonical API records with search, state/specialty/track filters, reported-position/name/state sorting, historical context, and canonical save actions.

The generated shell replaces misleading legacy wording with historically bounded language. `web/index.html` is asserted byte-identical rather than regenerated. The locked Fable source remains SHA-256 `1e1a16aa630449c9e763a04f6f720b51df0afa46822044de165687d7f8758987`.


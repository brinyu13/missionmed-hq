# Research Factory Adapter

The existing local RISE research factory remains the producer of record.

Fresh read-only state:

```text
CAMPAIGN = RISE-BOOTSTRAP-001
STATE = PAUSED_OPTIMIZATION_AUDIT
PROCESSOR = ultra
JOBS_TOTAL = 130
JOBS_COMPLETED = 1
JOBS_PARTIAL = 129
INGEST_STATE_STAGED = 130
REVIEW_QUEUE_OPEN = 130
ALERT = OPTIMIZATION_AUDIT_PAUSE
```

The P1_RISE_PARALLEL_COST_QUALITY_OPTIMIZATION_007 directory has no validated router output. No pause was overridden, no research was purchased, and no paid submission was made for QA. The isolated Railway app has no secure production bridge to the Mac-local SQLite/launchd factory, so Admin Research remains disconnected.

```text
ADMIN_RESEARCH_CONNECTED = NO
PARALLEL_FACTORY_STATE = PAUSED_OPTIMIZATION_AUDIT
UNCONTROLLED_SPEND = NO
```

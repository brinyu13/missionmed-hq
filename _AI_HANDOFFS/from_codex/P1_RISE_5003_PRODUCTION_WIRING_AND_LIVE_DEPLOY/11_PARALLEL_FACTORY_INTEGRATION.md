# 11 — Parallel Factory Integration

The current factory is a local launchd/Python/SQLite pipeline with raw results, normalized results, task/run IDs, and ingest staging. It has active writers but no canonical unattended RISE writer. Current evidence also includes payment/budget pause handling.

`P1_RISE_PARALLEL_COST_QUALITY_OPTIMIZATION_007` has no completed validated output, so no cost-aware routing policy was adopted. The proven factory interface must remain the future integration boundary; deployment of the student shell does not depend on optimizer completion.

No connection or provider mutation was made. Required activation controls are:

1. bounded canonical scope resolution;
2. server-selected task class/processor;
3. cost preview and explicit confirmation;
4. budget/cap enforcement and 402 handling;
5. immutable raw result and run provenance;
6. normalization plus deterministic validation;
7. safe auto-ingest only for exact, non-conflicting, authoritative facts;
8. review for identity, leadership, visa, closure, roster, and relationship ambiguity;
9. transactional corpus-version update and readback.

```text
PARALLEL_FACTORY_CONNECTED = NO
NEW_RESEARCH_AUTO_HYDRATES_RISE = NO
```

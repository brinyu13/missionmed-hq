# Y1-Y2-CAM-V6-3440 production gates

All items are deny-by-default. A local PASS is not a production claim.

1. File and independently verify an additive MissionMed OS decision naming the exact Supabase project/migration target, Railway project/environment/service, feature-off deployment command, rollback command, and anonymous health paths.
2. Apply and validate the offline migration only under that exact authority. Prove RLS/ACL, retention/deletion, backup/restore, concurrency, restart consistency, idempotency, and durable logout/upstream-session revocation.
3. Deploy feature-off. Verify deployment identity separately from route/TLS and application health. Ordinary students remain denied.
4. Bind a single Founder/admin entitlement and enable voice/admin-canary only. Video remains off.
5. Before any paid request, record the exact LiveKit origin, credential-presence booleans only, one reservation, zero retries, zero reconnect, `JRP_NEVER`, and cleanup readiness.
6. Engineering Test 1: Profile B only, at most 45 seconds, one reservation/dispatch/session, no retry or hidden reconnect. Reconcile provider-native and local duration/cost evidence before another paid test.
7. Engineering Test 2: at most 45 seconds and only if Test 1 leaves one explicit technical item. Otherwise skip it.
8. Founder Test 3: at most 59 seconds, Founder-only. No Test 4 or later. Profile A/ElevenLabs requires a separate acceptance receipt and approved voice ID.
9. Keep the video flag off unless Founder Test 3 passes. Any unconfirmed cleanup, cost mismatch, identity switch, stale entitlement, or revocation inconsistency trips the kill switch and closes admission.

The Founder-only terminal readiness phrase may be emitted only after steps 1–6 are independently evidenced and no technical blocker remains.

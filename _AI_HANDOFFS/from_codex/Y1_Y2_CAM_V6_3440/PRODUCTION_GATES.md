# Y1-Y2-CAM-V6-3440 production gates

All items are deny-by-default. A local PASS is not a production claim.

1. File and independently verify an additive MissionMed OS decision naming the exact Supabase project/migration target, Railway project/environment/service, feature-off deployment command, rollback command, and anonymous health paths.
2. Apply and validate the offline migration only under that exact authority. Prove the filed 45/45/59, active-video/Founder-Test-3, composite subject ownership, atomic reservation balance, single subject/test reservation, dispatch binding, safe pre-job refund, nonce/room/dispatch/job claim, termination request/observation, media-readiness, provider-cost, reconciliation, and kill-switch constraints plus RLS/ACL, retention/deletion, backup/restore, concurrency, restart consistency, idempotency, and durable logout/upstream-session revocation.
3. Deploy feature-off. Verify deployment identity separately from route/TLS and application health. Ordinary students remain denied.
4. Bind a single Founder/admin entitlement and enable voice/admin-canary only. Video remains off.
5. Wire the canonical HQ dispatch/observation controller and the child worker's concrete durable claim/termination-watch/join/reconcile gate to the exact approved database contract. Bind HQ stop to the atomic termination-request RPC and the child watcher to the exact job-bound termination signal. HQ must never own AgentSession or LemonSlice. Prove the dispatch nonce cannot admit a second subject/test reservation or a stale/foreign job; keep the worker's module-local default denial in place until then.
6. Wire the browser's scoped LiveKit connection and decoded-video/playable-audio readiness callback without persisting the room token. Before any paid request, record the exact LiveKit origin, credential-presence booleans only, one reservation, one participant, zero retries, zero reconnect, named `JRP_NEVER` dispatch, authorized-participant wait, REST termination, one terminal status/cost read, and cleanup readiness.
7. Engineering Test 1: Profile B only, at most 45 seconds, one reservation/dispatch/session, no retry or hidden reconnect. Reconcile provider-native terminal status/cost and local elapsed time before another paid test.
8. Engineering Test 2: at most 45 seconds and only if Test 1 leaves one explicit technical item. Otherwise skip it.
9. Founder Test 3: at most 59 seconds, Founder-only. No Test 4 or later. Profile A/ElevenLabs requires a separate acceptance receipt and approved voice ID.
10. Keep the video flag off unless Founder Test 3 passes. Any unconfirmed cleanup, cost mismatch, identity switch, stale entitlement, or revocation inconsistency trips the kill switch and closes admission.

The Founder-only terminal readiness phrase may be emitted only after steps 1–7 are independently evidenced and no technical blocker remains.

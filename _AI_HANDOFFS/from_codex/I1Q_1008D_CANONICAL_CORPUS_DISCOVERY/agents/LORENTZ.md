# Lorentz — boundaries, schema, and reconciliation

Verdict: **C1_OBSERVED; extraction blocked**.

Defined scoped surfaces and completeness grades, required identity-joined set reconciliation,
and separated source membership, artifact presence, speaker authority, and extraction
readiness. Recommended keyed aliases because raw hashes of low-entropy identifiers are
dictionary-attackable.

The current schema records candidate/consumer/local membership buckets, per-class artifact
availability, identity-binding class, content hash, structural counts, timestamp coverage,
and explicit overclaim boundaries. Stable current observation is not owner attestation.

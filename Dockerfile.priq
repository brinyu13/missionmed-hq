FROM node:22-alpine

WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci --omit=dev

COPY --chown=node:node . .
USER node
RUN npm run priq:build

ENV NODE_ENV=production
ENV PRIQ_WEB_ROOT=apps/priq-web/dist

EXPOSE 3000
CMD ["npm", "run", "priq:start"]

# Timeline Founder Re-anchor WordPress Agent Contract

For this subtree, D1-TIMELINE-FOUNDER-REANCHOR-015 and DR-127/DR-128 supersede
the repository-root StoryForge-specific `AGENTS.md` instructions. The root
contract continues to govern StoryForge paths and grants no authority over
Timeline.

This plugin may change only where an evidence-backed Timeline-owned repair
requires the isolated session, entitlement, JWT, same-origin gateway,
exact-version File Vault read adapter, consent, feature gate, or Matrix-return
integration. Preserve the current live rollback release and every deny-by-default
access boundary.

Do not modify WordPress core, generic authentication, shared Matrix assets,
navigation architecture, unrelated plugins, StoryForge, USCE, Arena, HQ, CDN,
DNS, public Cloudflare delivery, or unrelated data. Never log or commit secret
values. Production work requires exact Kinsta target verification, a fresh
backup, feature-off installation, canary, negative-access tests, kill switch,
and proven scoped rollback.

# Timeline RC1 WordPress Agent Contract

For this subtree, TIMELINE-RC1-STABILIZATION-001 and DR-021 supersede the
repository-root StoryForge-specific `AGENTS.md` instructions. The root contract
continues to govern StoryForge paths and grants no authority over Timeline.

This plugin may change only where an evidence-backed RC1 repair requires the
isolated Timeline session, entitlement, JWT, same-origin gateway, consent,
feature gate, or Matrix-return integration. Preserve the accepted Timeline
presentation and all current deny-by-default access boundaries.

Do not modify WordPress core, generic authentication, shared Matrix assets,
navigation architecture, unrelated plugins, StoryForge, USCE, Arena, HQ, CDN,
DNS, public Cloudflare delivery, or unrelated data. Never log or commit secret
values. Production work requires exact Kinsta target verification, a fresh
backup, feature-off installation, canary, negative-access tests, kill switch,
and proven scoped rollback.

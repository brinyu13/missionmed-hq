// Compatibility import for the internal diagnostic Studio. The canonical
// transport lives outside presentation code so candidate.2 and diagnostic
// surfaces consume the same proven capability.
export { LiveInterviewSession } from '../capabilities/live-interview.mjs';
